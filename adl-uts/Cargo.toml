[package]
name = "adl-uts"
version = "0.1.0"
edition = "2021"
rust-version = "1.92"
license = "MIT OR Apache-2.0"
description = "Versioned Universal Tool Schema contract; execution authority remains with ACC"
readme = "README.md"
include = ["src/**", "schemas/**", "Cargo.toml", "README.md", "LICENSE"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
schemars = "0.8"
