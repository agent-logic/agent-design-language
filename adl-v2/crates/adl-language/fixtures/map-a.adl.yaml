version: "0.5"
providers:
  alpha: {type: ollama}
  beta: {type: ollama}
agents:
  worker: {provider: alpha, model: test}
tasks:
  one: {prompt: {user: "one"}}
run:
  name: map-order
  workflow:
    kind: sequential
    steps:
      - {id: one, agent: worker, task: one}
