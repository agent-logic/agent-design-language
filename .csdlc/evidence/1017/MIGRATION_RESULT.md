# #1017 registration transfer result

All three owner-confirmed registrations (`v-dev.ai`, `v-fin.ai`, `v-sec.ai`) transferred from `default` to `agent-logic-admin`. Both account identities were authenticated and distinct; private identity and transfer evidence is retained outside Git with restricted permissions. The destination profile was the operator-designated company account and already held Agent Logic registrations.

AWS reports SUCCESSFUL for both source and destination operations for every domain. Fresh registration inventories show all three absent from source and present in destination. Destination details preserve auto-renewal, expiry, contacts, transfer locks, DNSSEC keys and nameservers. Full recoverable registration and zone snapshots were retained privately before transfer. Sensitive account/contact/transfer data is excluded from this packet.

Contrary to the initial expectation, each domain already had a public source hosted zone with only NS/SOA records. Registrar and public delegation matched these zones. All records remain byte-equivalent in structured before/after reads. No zones were created, modified or deleted. Public DNS baseline/post-transfer checks match after ignoring TTL countdown and answer ordering. No A/AAAA/MX or other service records exist in these zones, so live website/TLS/redirect/mail endpoint checks are not applicable to this observed configuration. This does not inventory unrelated services in either account.

The operator confirmed the exact three-name inventory and accepted the stated registration transfer with existing nameservers/zones preserved. The zones still belong to the personal source account. After verification, the operator accepted the result and deferred coming-soon websites to v0.93. This is the approved retained-source-DNS exception for #1017: leave the empty existing zones and delegation unchanged for now. Company registration/renewal control is complete; company DNS ownership and corporate contact replacement are not claimed. No DNS cutover occurred, so a propagation TTL window and obsolete-zone cleanup are not applicable. Revisit DNS ownership with the v0.93 website work; no future issue or milestone is created by this record. Repository integration and terminal closeout remain pending PR review and merge.

Recovery: registration transfer completed and its pending-request cancellation window no longer applies. Reversing ownership would require a new separately authorized transfer, not an automatic rollback. DNS recovery is unnecessary because the delegation and zone records were unchanged; preserved snapshots remain available. No resource cleanup was performed.

Evidence: `migration-verification.json`, `dns-baseline.json`, `dns-after.json`. AWS procedure: https://docs.aws.amazon.com/Route53/latest/DeveloperGuide/domain-transfer-between-aws-accounts.html . Mutation retries were disabled; every dispatch has an exclusive private intent record. A transient destination read for v-fin.ai was resolved by read-only polling, with no repeated mutation.

## Company contacts and renewal monitoring

Structured private comparison confirms all three domains already have COMPANY contacts naming Agent Logic. Admin, registrant and technical email addresses match the existing company domain agent-logic.ai. They were preserved intentionally; other contact fields need not exactly match that separate domain. The company account now controls registration and enabled auto-renewal. Retain the standard registrar renewal notifications to the existing registrant contact and Route53 operation status as the monitoring path; no new custom alarms or live notification-delivery test is claimed. See `renewal-contact-verification.json` and [AWS renewal documentation](https://docs.aws.amazon.com/Route53/latest/DeveloperGuide/domain-renew.html).
