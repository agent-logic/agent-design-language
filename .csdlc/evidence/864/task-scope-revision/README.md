# Issue #864 task-scope correction

Planning #1.1 resumed the bound `codex/864-v0922-wp01` worktree at operator direction after reviewing PR #865 at `d97f24621b05aae01fae09c531414c4cc9d52030`. No child issue, milestone, cloud resource or release is created here.

The operator requested eight task-family splits, eleven complete-result contracts, all seven planning tasks retained, and all active planning docs updated. The corrected denominator is 69 tasks, nine existing bindings and 60 prospective tasks.

`issue-scope-before.json` preserves the live pre-edit issue bodies; `issue-scope-proposed.json` is the exact bounded reconciliation for #852/#855/#862 and #864. Applying those bodies uses the native v3 GitHub owner after review; the proposal file alone is not remote-change proof.

Independent semantic review identified lost inherited acceptance, inverted stop conditions, qualification-before-integration, under-specified #855 acceptance and #852 historical criterion accounting. The revision preserves all 100 prior split-family obligations with explicit task owners, models stop conditions as actual failures, runs integration before independent qualification, preserves the five provider routes, and retains the historical five-row/19-finding boundary. Required OBS-S3 and ARCH-ADR retain their existing early non-gating status, with explicit final milestone-completion accounting.

Validation: deterministic local planning self-test, per-row graph/projection parity, baseline-obligation retention and no-partial-completion negative fixtures. These prove planning contracts only. Product, provider, Runtime and release execution are future tasks.

## Recorded scope reconciliation

Native v3 authenticated issue edits completed for #852, #855, #862 and #864. `remote-readbacks.json` verifies each title and body against the reviewed proposal; native receipts retain mutation/reconciliation evidence. No child was created.

## Independent review

`task_contract_review` passed semantic re-review: all 100 original obligations retained, 69 nodes acyclic, nine issue identities and seven planning tasks preserved, full provider and criterion requirements retained, and integration precedes qualification. Its requested #855 title correction was applied. `docs_final_review` passed after completing task-title/outcome parity and correcting the SIM package count. The planning self-test passes 75 negative fixtures. Exact commit review follows card finalization; this paragraph records precommit review only.
