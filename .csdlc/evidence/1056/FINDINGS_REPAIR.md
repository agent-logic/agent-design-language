# PR 1063 authorization, provenance, and provider response repair

The three blocking P2 findings at 8ae29a749bf41cce7e687f723308cf1ca87d687f are repaired. Authentication runs before body polling, and POST reauthenticates after JSON extraction so expiry or revocation during upload cannot authorize admission. The server embeds its Git revision and production-input cleanliness at compilation, rejects mismatched configuration, and preserves that identity when copied; adjacent provenance JSON cannot relabel it. Production compile-time resources participate in both cleanliness and Cargo rerun tracking.

Provider HTTP bodies are read incrementally with a 4 MiB cap, an 8 KiB scratch buffer, and a one-byte overflow sentinel. Success and error replies, MiniMax, and Ollama identity metadata use the bound. Bedrock installs the SDK body limit before deserialization/blob aggregation. Neither hosted nor local operation paths persist an oversized provider result. Local-model results and completed operations report the actual provider invocation model identity and embedded candidate revision for client verification.

Validation: eleven server integration tests passed at ab8dcbdec989505b32fd614312e60731d4fd0029, including unpolled unauthorized bodies, valid-auth malformed JSON, delayed-body revocation/expiry, dirty compiled-resource provenance, copied executable identity, and oversized hosted/local failures. Five focused bounded-provider unit tests passed, covering exact cap/overflow, HTTP and MiniMax success/error replies, Ollama metadata, and SDK body/client interception with fake transport. Independent review by review_1056_server cleared that exact source head after its two additional P2 findings were fixed. Native proof/review/publication and remote CI remain separate current-head records.

PVF: the new provider_adapter::tests::bounded_* tests belong to the runtime lane, have a deterministic negative-boundary proof role, use bounded in-memory/loopback fixtures and no provider spend, and support the Beta release gate alongside the server fixture PVF manifest. Build-provenance fixtures require the declared Rust toolchain and Git. This component proof does not establish deployed TLS, real-provider acceptance, website/local-agent integration, merge readiness, or Sprint 10 completion.

Native proof at 68750d357f reported a server test failure; its bounded stdout excerpt omitted the assertion. Repeating the produced executable reproduced `WouldBlock` in the mock provider request read. Accepted sockets can inherit the nonblocking listener mode on macOS. The fixture now explicitly enables blocking reads before its existing three-second timeout. All eleven tests passed afterward, and twenty full-suite repetitions passed (220 tests). This deterministic test-harness correction changes no production behavior or acceptance assertions. The failed native receipt and reproduction log remain retained; a fresh candidate requires fresh native proof. Focused clippy also passed before this test-only correction.
