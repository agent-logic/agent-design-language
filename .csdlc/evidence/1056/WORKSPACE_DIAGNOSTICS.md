# PR 1063 coverage cancellation diagnostics repair

At b34a32da6dee43b0efd04e3da176d238f762f82b, run35377358543 workspace job105705214322 exceeded its30-minute limit. Instrumented compilation consumed19m07s after cache warmup reported no source target. Runtime coverage succeeded; the aggregate correctly failed because workspace coverage was cancelled. Existing artifacts omitted per-partition progress because the runner prints partition logs only after all children terminate. No failing assertion or hung-test identity can be inferred from the retained output.

The repair adds the existing current-run workspace partition-log directory to the existing always-run artifact upload. It changes no timeout, test selection, threshold, producer aggregation, or runner execution. The native SPP plan amendment was accepted and validated before the workflow edit. Only subsequent CI can demonstrate artifact retention during real cancellation and diagnose any remaining timeout.

Focused validation passed: test_ci_runtime_contracts.sh (including producer-result and PR-fast coverage checks) and test_coverage_authority_contract.sh. The new contract assertion checks the full workspace job specifically, including always-upload semantics and exact current-run directory. PVF: tooling/validation-diagnostics contract proof; deterministic static workflow validation and bounded fake-runner fixtures; no provider/network spend; supports the required CI gate without constituting coverage success. Native proof/review/publication and exact-head remote CI are separate records. The PR remains draft pending coverage and deployed acceptance.
