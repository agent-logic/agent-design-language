# #1044 bounded setup repair

Native first preparation previously treated authenticated completed issue edits as local lifecycle residue and read a nonexistent index. The change exempts only exact repository/issue edit intent+receipt pairs accepted by the existing native verifier. Immutable receipt bytes remain in place; local residue and unsupported, pending or damaged remote state continue to block.

Validation: 27 semantic_gate_a transaction tests passed, including new positive first-preparation/history-preservation proof and ten negative cases. Synthetic deterministic local CPU/filesystem contract tests; no cloud/service proof. `git diff --check` passed. An isolated native owner binary is installed under this worktree's .adl/bin/native-v3 with installer provenance; the shared binary was not replaced.

Independent subagent review of the changed source/test diff reported no actionable correctness defect. This is a setup-only repair handoff, not publication, merge, terminal completion, or approval for #1017 cloud migration. #1017 preparation and binding verification will be recorded separately.
