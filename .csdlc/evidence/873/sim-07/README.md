# Issue #873 independent qualification

This packet evaluates candidate `f31b1b710074fdb51f41400b32b79c633bc3336e` with the frozen issue-specific installed binary. It used isolated fixture repositories, synthetic remote transports, and both primary and genuine linked-worktree entrypoints. It did not replace the shared owner binary, activate writers, call live providers, or mutate GitHub.

The two exact installed-binary journeys passed with 35 retained attempts. The 27 declared integration targets and five exact library regressions contributed 252 unique passing tests, with no unexpected test failure. All 33 emitted result envelopes have correlation IDs; two deliberately interrupted attempts have attempt IDs and no result envelope. The corrected warm-host prepared-start samples were 3.865 s, 3.778 s, and 3.954 s, all below the three-minute fixture target. Every candidate timing sample records the frozen installed binary BLAKE3 `c6d7c79a7652dfa1d73c3ce8437957b86cc4ea17e2ad5d322f7a452fd052835d`. The measurement does not establish OS-cold performance or human authoring time.

The retained #872 conversion evidence validator passed. After #1021 merged the portable generation-9 fixture and corrected harness, the separately required ignored old-owner release gate passed in an isolated rerun: one Cargo test covered seven roles, 12 scenarios, and 30 fault cases in 636 seconds. That run used the corrected #1021 binary (SHA-256 `f52c2915fbd708b6c78a7ce20262a3bde276caf188c35336f2c4b422ca867e8e`), not frozen qualification candidate `f31b1b710074fdb51f41400b32b79c633bc3336e`; it is preserved as separately bound evidence and does not satisfy the frozen-candidate conversion or finite-recovery gates. No live conversion, shared installation, or writer activation occurred.

## 2026-09-16 requalification checkpoint

The branch was advanced to observed source head `8b482221e6a6c0eccab1175c2320aa1f7d441ce1` without changing the frozen installed candidate identity or digests. The bounded focused corpus contains 37 exact result records: 33 counted-test passes, one passing zero-test validator, two passing exact installed journeys, and one passing non-test target probe. Their test summaries total 256 passed, zero failed, and one ignored. Because the packet combines retained frozen-candidate records with refreshed records, it does not claim that all 256 passing outcomes ran at one exact source head. Every summarized hash resolves to an included raw capture and result record.

The retry comparison remains `not_proven`. Fifty-five retry-contract tests passed and the accepted predecessor completed both declared journeys in 34 attempts. The frozen candidate refused the authority-valid unconverted fixture at `prepare` with `semantic_prepare_refused` / `LegacyMigrationRequired`, so it emitted no candidate ledger and the comparator correctly refused to calculate a percentage. Detailed operational retry diagnostics remain locally retained and are intentionally outside this bounded evidence change.

The candidate-first prepared-start replay completed in 6.588 seconds, below the source contract's three-minute target. The artifact identifies the condition as the first measured fixture and process on an already warm host. It makes no OS-cold claim; OS-cold evidence is not a prerequisite in the canonical issue contract.

The overall decision remains `not_proven` because frozen-candidate conversion and finite recovery, comparable retry reduction, independent exact-head review, and evidence-PR CI are not yet proven.

`scorecard.json` and `qualification-decision.json` contain the adjudication. Raw commands, stdout, stderr, result records, exact installed attempt ledgers, and timing evidence are retained alongside them.

Replay scripts derive the repository root and accept machine-specific binary, worktree, target, and evidence locations through named environment inputs. Authored summaries and metadata use logical or repository-relative references while candidate and predecessor identity remains bound by revision and cryptographic digests. Raw execution records under `commands/`, `results/`, `observations/`, `secondary/`, and the detailed timing artifact retain exact execution paths when those paths were part of the original observation. Historical raw captures also retain their original trailing whitespace and final blank lines because their SHA-256 values are recorded in result records. Diff hygiene therefore checks authored JSON, Markdown, and scripts and exempts immutable raw captures; refreshed command captures use normalized spacing and refreshed bound hashes.

The prepared-start replay binds its Cargo build, harness discovery, candidate slot, and execution to `ADL_ISSUE873_TIMING_TARGET_DIR`. A retained build-only probe passed with a non-default isolated target and found both the timing harness and candidate slot beneath that selected target.

## Follow-up measurement harnesses

`run-special-corpus.sh` now separates ordinary build-and-run from a bounded execute-only replay. Execute-only admission requires `ADL_ISSUE873_TIMING_VARIANT=candidate` and `ADL_ISSUE873_TIMING_REPETITIONS=1`; it retains a separate `prepared-start-candidate-first-unadjudicated.json` artifact and exits before conversion gates. The runner retains that artifact only after the measurement process succeeds, the frozen candidate digest matches, and every retained sample meets the fixture timing target. This mode proves only that the candidate-first single-sample selection is reproducible. It does not identify the host as OS-cold or change the existing timing decision.

`retry-scenario-map.json` freezes the accepted predecessor and candidate identities plus one semantic denominator for the two installed journeys. Each scenario binds its initial fixture facts and fake-remote schedule; each step binds the retained installed command shape, variant-specific fixture operand, intended operation, accepted and completing outcomes, and retry policy. `validate_retry_comparison.py` rejects provenance drift, scenario or relevant-fact drift, missing steps/outcomes, uncorrelated result envelopes, ledger-supplied retry classifications that disagree with the preceding outcome and declared policy, and journey status that disagrees with every step's final outcome. The synthetic fixtures under `tests/` exercise the validator; they do not prove the operational retry-reduction gate.

`run-retry-qualification.sh` is the operational adapter. It runs in three explicit phases so each binary can be inspected before the next step: `predecessor` creates a new run directory, `candidate` adds only candidate artifacts after the predecessor ledger exists, and `compare` writes the result only after both ledgers exist. It verifies the selected binary's SHA-256 identity, uses explicit dual-binary drivers for the ordered 13-step local and 18-step shared terminal scenarios, and restores the compiled capture slot with an outer interruption trap. Normalization preserves the raw observations by digest, binds issue and cleanup topology identities, derives outcomes and retry classes from retained result envelopes, and rejects oversized inputs, missing, reordered, noncontiguous, or undeclared behavior before the comparator runs.

`measurements/retry-reduction/` is the selected operational packet. The predecessor completed both journeys with 34 attempts and three avoidable retries. The frozen candidate did not produce a comparable ledger: it refused the authority-valid source fixture at prepare with `semantic_prepare_refused` / `LegacyMigrationRequired`. An audit of 89 retained fixture roots found no authority-valid clean baseline without the tracked issue-505 terminal receipt. The two clean roots from which that receipt had been removed both retain `operational_context_required` because their authenticated authority became stale. `run-status.json` therefore records `not_proven`, a null comparison payload and retry percentage, and no 50 percent claim.
