#!/usr/bin/env ruby
# frozen_string_literal: true

abort "usage: validate-native-receipts.rb <linux.json> <macos.json>" unless ARGV.length == 2

warn "native receipt validation is intentionally unavailable before issue #209 implementation"
exit 1
