# Retired fixture format

These files are review-history examples only. The executable suite now builds
a valid fixed-disposition packet and mutates the production `validate_packet!`
path, including fresh execution of the immutable validator blob.
