# Retired fixture format

These JSON files are retained only as review-history examples. They are not
executed and are not validation authority. `test-production-validator.rb`
builds a complete packet and mutates the same `validate_packet!` path used in
production.
