#[test]
fn conversation_rooms_implementation_sentinel_fails_closed() {
    panic!(
        "issue #115 conversation_rooms is a bind-readiness sentinel; implement the governed room contract after gates #111, #112, and #113 are terminal, merged, ancestral, and handed off"
    );
}
