{
  "schema": "codefriend.agent.pvf.v1",
  "issue": 1058,
  "lane": "runtime",
  "proof_role": "component_authorization_no_replay_reconnect_aggregate_retention_and_cli_dispatch_guards",
  "determinism": "controlled clock, loopback HTTP faults and private local filesystem",
  "resource_profile": "bounded local CPU/disk and loopback fixtures including four 1.4MiB lane outputs; no real provider calls",
  "release_gate": "required component evidence, insufficient for installed/provider/browser acceptance",
  "command": "cargo test --manifest-path adl/Cargo.toml --test codefriend_agent --bin codefriend-agent",
  "additional_commands": [
    "cargo test --manifest-path adl/Cargo.toml --lib journal_drop_releases_lock_with_duplicate_descriptor_alive"
  ],
  "journal_lock_proof": "Deterministic duplicate-descriptor ownership test: exclusion before owner drop, release despite retained duplicate, and continued exclusion by replacement owner.",
  "original_store_integration": {
    "issue": 914,
    "lane": "runtime",
    "proof_role": "original admission ownership across actual four-lane reconnect and retained payload cleanup",
    "determinism": "existing controlled clock and bounded loopback agent fixtures",
    "resource_profile": "local CPU/disk and loopback only; no provider calls",
    "release_gate": "required component proof; not full installed website or independent qualification",
    "cases": [
      "exact original Store/admission identity survives reconnect",
      "missing or tampered original Store denies further dispatch without re-admission",
      "expiry and confirmed unpair scrub original Store while preserving no-replay reservations"
    ]
  },
  "legacy_observation_deadline_regression": {
    "issue": 1102,
    "test": "known_running_operation_resumes_after_legacy_deadline_without_cancel_or_repost",
    "lane": "component",
    "proof_role": "customer cycle continues known Running operation despite elapsed legacy observation timer",
    "determinism": "controlled loopback and clock; persisted acknowledgement seeded with elapsed old deadline while authority remains live",
    "resource_profile": "four synthetic local lane responses; no external provider calls",
    "assertions": [
      "first invocation dispatches exactly one lane and loses GET observation",
      "restart reads original Running acknowledgement with past legacy deadline",
      "each distinct lane POST exactly once; no cancellation POST; completed identical retained report"
    ],
    "executed": true,
    "execution_evidence": ".csdlc/evidence/1102/language-foundation/idle-policy-validation.json"
  }
}
