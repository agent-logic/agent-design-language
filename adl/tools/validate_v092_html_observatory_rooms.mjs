import assert from "node:assert/strict";
import test from "node:test";

test("issue #115 rooms browser validator implementation sentinel", () => {
  assert.fail(
    "implement the dedicated rooms browser validator after gates #111, #112, and #113 are terminal, merged, ancestral, and handed off",
  );
});
