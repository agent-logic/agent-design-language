process.stderr.write(
  "issue #113 HTML Observatory roster proof is not implemented; replace this fail-closed sentinel with the live Runtime-backed validator after gates #83 and #142 pass\n",
);
process.exit(1);
