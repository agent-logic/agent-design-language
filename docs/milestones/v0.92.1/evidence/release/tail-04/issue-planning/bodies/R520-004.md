## Summary

Repair the P1 contradiction between the completed C-SDLC v3 cutover and current operator-facing docs, CLI help, module/package metadata, and assertions that still direct operators to pre-cutover v2 authority.

## Goal

Establish one coherent post-cutover C-SDLC v3 authority statement on every current routing surface while retaining old records only as historical evidence.

## Required Outcome

At exact review candidate `c24f8fa65ce445b03ce6cd69007307291d78b60c`, finding `A520-ARCH-003` shows that root policy declares PR #591’s completed cutover while linked notice, CLI, module, package, and test surfaces still require the opposite pre-cutover routing. Every current operator surface must agree with authenticated selector and receipt truth.

## Deliverables

- Consistent post-cutover operator guidance in root and nested policy, current docs, CLI help, module docs, and package metadata.
- Updated command-manifest assertions for the current authority state.
- Clear historical labeling for immutable pre-cutover evidence.
- A cross-surface fitness check that rejects obsolete current-routing language when the selector proves post-cutover authority.

## Acceptance Criteria

- All current operator-facing surfaces identify typed C-SDLC v3 as operational authority after the completed cutover.
- No current link presents the historical changeover notice as live routing authority.
- Pre-cutover records remain immutable and unmistakably historical.
- The fitness check fails when current help or policy contradicts authenticated selector/cutover state.
- Selector and cutover receipt validation pass against the asserted authority state.

## Repo Inputs

- Source finding: `A520-ARCH-003` from internal review #520.
- Exact reviewed candidate: `c24f8fa65ce445b03ce6cd69007307291d78b60c`.
- Evidence: `AGENTS.md:3-33`, `csdlc-v3/AGENTS.md:1-19`, `docs/csdlc-v3/TOOLING_CHANGEOVER_NOTICE.md:3-17`, `csdlc-v3/src/main.rs:290-294`, `csdlc-v3/src/main.rs:586-601`, `csdlc-v3/src/lib.rs:1-8`, `csdlc-v3/src/commands/local/mod.rs:1-6`, `csdlc-v3/Cargo.toml:1-6`, and `csdlc-v3/tests/command_manifest.rs:219-249`.
- Concrete defect: current surfaces simultaneously declare v3 operational and prohibit v3 binding, publication, finish, cleanup, and GitHub mutation.
- Owned paths: the listed current policy, help, module, package, and fitness-test surfaces; immutable historical records are read-only inputs.

## Dependencies

- Parent remediation issue: #522.
- Internal review source: #520.
- Coordinate with `R520-006` and `R520-007` so proof-route behavior and detached-head tests use the same authority model.

## Demo Expectations

- Show the installed CLI’s current help, authenticated selector/receipt validation, and the cross-surface fitness check agreeing on one post-cutover route.

## Non-goals

- Do not rewrite immutable historical evidence.
- Do not restore v2 as default authority.
- Do not weaken authority authentication or reconciliation requirements.

## Issue-Graph Notes

- Remediation candidate: `R520-004`.
- Severity: P1.
- Parent ledger: #522; source review: #520.
- Coordinate rather than duplicate checkout-identity repair in `R520-006` or fixture repair in `R520-007`.

## Notes

- Prose repair alone is insufficient; the fitness check must prevent recurrence.

## Tooling Notes

- Use the active typed C-SDLC v3 lifecycle despite stale strings being repaired.
- Card changes must use the corresponding editor skills and native edit/validate routes.
- Run focused command/help/selector tests, an obsolete-guidance scan, bounded pre-PR subagent review, and native review/publish/finish/clean.
