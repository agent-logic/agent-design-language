## Summary

Repair the P1 detached-head C-SDLC v3 cutover and rollback regression suite so the required standalone lane has a reliable passing baseline without weakening production authority guards.

## Goal

Make all cutover and rollback fixtures self-contained and prove the complete locked C-SDLC v3 suite from a detached exact-head checkout.

## Required Outcome

At exact review candidate `c24f8fa65ce445b03ce6cd69007307291d78b60c`, finding `T520-TEST-001` reproduces ten failures in the required C-SDLC v3 suite. The fixtures combine a real checkout with historical #505 branch state and are blocked by the production doctor guard. The repair must isolate fixture topology while retaining the real fail-closed behavior.

## Deliverables

- Self-contained cutover and rollback fixtures independent of historical branch/worktree topology.
- Passing results for all ten named failing regressions from a detached exact head.
- Negative coverage showing genuine stale or mismatched authority still fails closed.
- Complete local locked-suite evidence and the required hosted `csdlc-v3-standalone` result.

## Acceptance Criteria

- The shadow doctor fixture uses only isolated, fixture-owned repository, branch, registration, and authority state.
- All ten cutover/rollback regressions pass from a clean detached exact-head checkout.
- Production doctor and authority guards still reject real stale, mismatched, unmerged, or unauthenticated state.
- `cargo test --locked --manifest-path csdlc-v3/Cargo.toml` passes in full.
- The required hosted standalone lane passes at the same published head.

## Repo Inputs

- Source finding: `T520-TEST-001` from internal review #520.
- Exact reviewed candidate: `c24f8fa65ce445b03ce6cd69007307291d78b60c`.
- Evidence: `.github/workflows/ci.yaml:191-215` and `csdlc-v3/tests/terminal_cleanup_cutover_commands.rs:1365-1446`.
- Exact reproduction: the full locked suite exited 101; `terminal_cleanup_cutover_commands` passed 20 tests and failed 10, each with `ProofRouteStatus::Blocked`, `shadow_command_not_successful`, and child doctor exit status 2.
- Failing cases: `approved_cutover_atomically_installs_selector_and_rollback_receipt`, `approved_cutover_refuses_existing_receipt_before_mutation`, `authenticated_cutover_rejects_unmerged_authority_pr`, `cutover_and_rollback_share_one_mutation_lock`, `cutover_recovers_interrupted_boundaries_and_rollback_is_idempotent`, `cutover_rejects_intermediate_output_parent_symlink_escape`, `executable_cutover_cannot_bypass_request_findings`, `executable_cutover_requires_authenticated_github_authority_before_mutation`, `rollback_fails_closed_on_stale_selector_digest`, and `tracked_selector_revert_rolls_back_from_fresh_worktree`.
- Owned paths: `csdlc-v3/tests/terminal_cleanup_cutover_commands.rs` and only the tightly coupled CI/test-fixture surfaces required by the repair.

## Dependencies

- Parent remediation issue: #522.
- Internal review source: #520.
- Coordinate with `R520-004` and `R520-006` because the suite exercises authority and checkout identity.

## Demo Expectations

- From a clean detached checkout, run the complete locked suite and the stale/mismatch negative cases; retain the exact command, revision, and results.

## Non-goals

- Do not disable, ignore, or reduce the failing test set.
- Do not weaken production doctor or authority guards to satisfy fixture setup.

## Issue-Graph Notes

- Remediation candidate: `R520-007`.
- Severity: P1.
- Parent ledger: #522; source review: #520.

## Notes

- The required baseline is currently deterministically red, so focused passing tests elsewhere are not sufficient evidence.

## Tooling Notes

- Use typed C-SDLC v3 in a bound FastWork worktree.
- Use an external target directory or the approved warm-cache wrapper; cache evidence is not test proof.
- Run the complete locked suite once locally at the final head, then rely on the required hosted lane rather than duplicating broad checks repeatedly.
- Obtain bounded pre-PR subagent review before native review/publish/finish/clean.
