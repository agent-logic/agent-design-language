## Summary

Remediate P2 finding `T520-DEP-002` from internal review #520 by pinning the Rust compiler used by required CI. At reviewed candidate `c24f8fa65ce445b03ce6cd69007307291d78b60c`, the toolchain installer action is commit-pinned but requests floating `stable`, and the repository declares neither a tracked toolchain version nor an explicit manifest compatibility floor.

## Goal

Make required Rust validation reproducible for identical repository bytes and turn compiler updates into explicit reviewed dependency changes.

## Required Outcome

Required local and hosted Rust lanes use one explicit supported compiler version, the intended MSRV is declared where appropriate, and validation proves CI no longer resolves a floating stable channel.

## Deliverables

- A tracked Rust toolchain declaration or equivalent single authoritative explicit compiler pin.
- Required CI updated to consume the pinned compiler rather than floating `stable`.
- Manifest `rust-version` declarations where needed to express the intended MSRV contract.
- A focused regression check or workflow-policy assertion that rejects floating Rust channels in required CI.
- Exact-version test, Clippy, formatting, and toolchain-record evidence.

## Acceptance Criteria

- Required CI resolves an explicit supported Rust compiler version.
- Required local validation resolves the same compiler version without relying on the operator's ambient default.
- The intended MSRV is declared consistently where package metadata owns that contract.
- A compiler update requires a visible reviewed repository change.
- Required Rust tests, Clippy, and formatting pass with the pinned version.
- The change does not include an unrelated edition or dependency upgrade.

## Repo Inputs

- Internal review #520 and source finding `T520-DEP-002`.
- Exact reviewed candidate `c24f8fa65ce445b03ce6cd69007307291d78b60c`.
- `.github/workflows/ci.yaml:203-206`, which pins the action commit but requests `stable`.
- Rust package manifests including `adl/Cargo.toml`, `csdlc-v3/Cargo.toml`, and `adl-runtime/Cargo.toml`; the reviewed manifests contain no `rust-version` floor.
- The absence of a tracked `rust-toolchain` or `rust-toolchain.toml` at the reviewed candidate.
- `docs/milestones/v0.92.1/evidence/release/tail-04/specialists/dependency-review.md`.

## Dependencies

- Parent remediation issue: #522.
- Internal review source: #520.
- Coordinate with remediation candidate `R520-007` before accepting a new C-SDLC v3 validation baseline, because that candidate repairs an exact-candidate suite failure.

## Demo Expectations

- No product or cloud demo is required.
- Show the resolved compiler version in both local and hosted validation evidence.
- Show a negative policy case in which a required workflow requests floating `stable` and is rejected.

## Non-goals

- A Rust edition migration.
- Opportunistic Cargo dependency upgrades.
- Exhaustive optional-feature coverage.
- Weakening or skipping any currently required CI lane.

## Issue-Graph Notes

- This is remediation candidate `R520-010` under #522.
- It resolves only `T520-DEP-002`.
- Order final baseline proof after `R520-007` when the same C-SDLC v3 suite is part of both acceptance surfaces.

## Notes

- Severity: P2.
- Source role: dependencies.
- Confidence: high.
- Owned paths: `.github/workflows/ci.yaml`, the selected tracked toolchain file, applicable Cargo manifests, and the narrowly coupled CI policy test/evidence.
- Select the version from actual repository compatibility evidence; do not infer that the newest compiler is automatically the supported compiler.

## Tooling Notes

- Bootstrap and execute through typed C-SDLC v3 in a bound FastWork worktree.
- Use the stable generated owner binaries rather than worktree-local build output for lifecycle operations.
- Run the complete required Rust test, Clippy, and formatting lanes with the pinned version and record the exact version.
- Obtain bounded independent review before publication and bind it to the final exact head.
