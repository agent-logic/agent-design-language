# TAIL-03 publication candidate

Issue #519 finalizes one publication-candidate packet from the reviewed #518 / PR #753 handoff. PR #753 merged as `d0907106e1202710586b34317eaf98ddc8b4cb8a`. Its published head is `4db787bd9e14f42411435e9bbc3c57af6fa5664b`; independent review approved substantive source `13d6afcee7e4f0050491b8c740547fdb9dac5f3d`. These identities remain separate.

`candidate.json` binds the published source head, its independently reviewed source, the source review-record digest, and every TAIL-02 artifact hash. The source handoff covers 791 documents. Read immutable source artifacts with `git show <source_head>:<path>`; read the current manifest-referenced document bytes from the checked-out paths and verify them against `handoff-content.json`. Validation proves that review-scoped source files are unchanged from the reviewed source, that TAIL-02 artifacts agree with the merged candidate on canonical `origin/main`, and that every manifest-referenced document byte stream is hash-bound and redaction-scanned before final acceptance.

`source-pr.json` retains the observed merged PR, merge time, exact head and closing body. Its body preserves a historical pre-merge hold; the live merged state is the current observation. The source PR closes #518. The eventual #519 implementation PR must use `Closes #519`, without closing #518 or another release-tail issue. Its generated PR number belongs in typed publication records; the candidate's null #519 PR number intentionally avoids self-referential packet rewrites after review.

Run `ruby .csdlc/prepared/issues/519/validate-publication-candidate.rb --all` for the complete final contract, or the declared `--linkage`, `--exact-head` and `--redaction` lanes. `--redaction` scans the TAIL-03 packet and all 791 manifest-referenced documents while reporting only path, line, and pattern class for unsafe content. `--self-test` additionally proves rejection of altered hashes, incorrect closing relationships, unmerged observations, wrong heads, wrong repository identity, premature preparation claims, and redaction fixture classes. Refresh the read-only PR observation before final publication review. The saved observation is evidence of one read, not a continuous monitor.

The historical quality assessment and current accounting dispositions from #517 remain source evidence. Accounting resolution supplies neither missing product proof nor release approval. This task performs no merge, tag, release or issue-close mutation and changes no package versions. Runtime, cloud qualification and release approval retain their existing owners.

Validation classification: deterministic local documentation contracts; small CPU/file/Git reads; issue-level proof. Hosted CI is integration evidence and does not replace these local checks. Redaction screening covers the publication packet and every manifest-referenced document byte stream, but remains a finite pattern gate rather than proof that no private payload can exist. The source review record retains its exact scope and residual risks. Final #519 independent review and publication state are recorded through typed SRP/SOR authority.
