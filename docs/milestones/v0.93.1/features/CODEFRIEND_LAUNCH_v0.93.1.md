# v0.93.1 CodeFriend Beta 1 Launch

## Metadata

Planning template family 1.1.0; authoring issue #922. Scope split approved by the operator; draft package, not opened, executed or release-approved. Named execution owners and resource limits remain to be assigned.

## Template Rules

Preserve product requirements; structure checks are not implementation proof.

## Purpose

Launch the complete qualified Beta 1, including full branded document, review, test and 4+1 architecture output.

## Context

The [original launch plan](../../v0.93/features/CODEFRIEND_LAUNCH_v0.93.md) retains source provenance for the Drive Beta 1 second-pass brief and seven workstreams. The operator-approved split moves launch to v0.93.1, after repository portability, and removes the blanket v4 prerequisite.

## Coverage / Ownership

Private codefriend owns product software; existing private codefriend.ai owns the website. Runtime is an independently versioned producer, consumed through a pinned compatible release.

## Overview

CF-01 accepts requirements/evidence mapping and launch target; CF-02 delivers the deployable product; CF-03 completes external onboarding; CF-04 qualifies operations/recovery; CF-05 independently qualifies the candidate; CF-06 prepares website handoff; CF-07 performs explicitly authorized live launch.

## Design

Map all seven source workstreams: repository engine, specialist reviews, diagrams, targeted test generation, documentation, report/safety gates and minimal product experience. Cover ADL self-review, external OSS and PR-review demos. Both hosted and local-agent website modes plus CLI remain supported qualification surfaces. Architecture output includes logical, process, development and physical views plus scenarios, with shared IDs and consistent evidence.

## Execution Flow

RD-11 → CF-01 → CF-02 → CF-03 → CF-04. CT-01–CT-10 supplies complete templates; CT-05 and CF-04 feed INTEGRATE. CF-05 reuses #1150 to qualify that pinned lockset after accepted #1148/#1149, then CF-06 and CF-07 follow. QUALIFY audits that evidence and live launch without a duplicate campaign. CF-02 does not depend on RV-08 or another v4 result.

## Determinism and Constraints

Pin exact product, existing Runtime, template and website versions. Retain failed/unknown source evidence and uncertainty reservations; never blind-replay unknown requests. Keep repo intake, privacy, data retention/deletion, scope and authority visible.

## Integration Points

Consume split artifact contracts without sibling checkouts. Reconcile existing codefriend.ai issue #1 before creating overlapping website execution work. CF-02 demonstrates actual existing Runtime compatibility, rather than assuming it or requiring unrelated platform expansion.

## Validation

Existing #1148 (citation-grounded correctness) and #1149 (uncertain second-run recovery) are additional launch prerequisites. CF-05 reuses existing #1150 for independent qualification after scope alignment; do not seed a duplicate CF-05 issue. The 43 core work packages therefore map to 45 distinct planned issue identities including those two additions: three identities already exist and at most 42 core issues need seeding. Preserve their identities and original #915 failures; do not create duplicate tasks or replay an uncertain request to manufacture a result. Retain all 12 original qualification cells and 24 obligations; independently verify installed success, refusal, cancellation, interruption, recovery, cost controls, deletion and rollback. Six exports across three formats are export evidence, not accepted review quality.

## Acceptance Criteria

An authorized external tester completes repository intake through source-grounded reports, diagrams, meaningful generated tests and documentation, with readable branded exports and explicit limits. Every source external-tester criterion maps to exact evidence or a launch blocker. CF-07 needs explicit candidate/audience/environment/budget authorization and then live verification; preparation cannot count as launch.

## Risks

Source-free or inexact citations can make polished output misleading. Unknown runs can consume cost without a completed result. Independent qualification and conservative reconciliation remain launch gates.

## Future Work

Runtime v4 and platform expansion move to v0.93.2. Any demonstrated Runtime defect blocking these admitted journeys needs a bounded result and proof; do not silently import all v4 work.

## Notes

Scope-approved draft; no provider call, product deployment, public artifact or release is authorized by this planning document.
