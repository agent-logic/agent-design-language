# v0.92 Article Series

This directory retains the completed editorial packet for the v0.92 article
series.

Each numbered article directory contains `SOURCE_PACKET.md`, `ARTICLE.md`, and
`EDITORIAL_REVIEW.md`. The series root also contains
`SERIES_ARC_AND_CLAIM_MATRIX.md` and `PUBLICATION_DISPOSITION.md`.

The tracked validator at `.csdlc/evidence/5844/validate-article-series.rb`
checks this packet shape and rejects placeholders, unsafe private paths, and
publication claims that lack an explicit operator-approved disposition.

The packet is complete as editorial work. Publication in any external channel
remains governed by its recorded disposition and operator authorization.
