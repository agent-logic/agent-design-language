# Independent packet review — changes required

P2 **PACKET-IND-001:** portable proof references do not resolve. Findings and acceptance rows retain `lanes/*` paths, while the publication tree has `specialist_reviews/` and `coverage/`; the local diagnostic scripts/results cited by security/tests/demos/code and the website test log are absent. The evidence coverage also uses the wrong retained-census filename. Publish sanitized, identity-bound proof records and map paths, or explicitly mark local-only unavailable proof. Replace the personal absolute dependency-helper path with a portable external-tool identity and version/hash.

P3 **PACKET-IND-002:** historical support count is overstated. The actual ledger has 51 primary, 219 indexed supporting and 20 unavailable entries. The report's 239 indexed count combines the latter two. Preserve the explicit unavailable category.

Verified all26 actionable source-lane findings survive consolidation, with4 P1/20 P2/2 P3 and no withdrawn CODE-003. Scope/severity language retains historical supply-chain findings and distinguishes weak test oracles from observed production failures. All nine specialist reports disclose their execution limits and shared reviewer identities. Product findings are not falsely marked repaired. The lead-authored917 independence boundary and draft918 acceptance distinction are retained.

Verified366 criterion rows remain nonqualification claims;4778 artifact cohort counts,116 ancestry rows (108 ancestor/eight no merge), and29 historical finding crosswalk rows match their ledgers. These checks validate synthesis consistency, not renewed source-byte provenance or every historical criterion. The acceptance ledger's generic source-component observations are candidly not criterion-specific installed proof.

The reviewed snapshot explicitly leaves final source reconciliation and privacy/quality checks pending, so this review does not approve a full-completion claim. Tests report still refers to pending transactions.rs; reconcile that status when merging final coverage. Code, architecture and the docs supplement were authored by this reviewer; they are not given a second independent approval here. Other-lane reports and lead synthesis were independently inspected. No product edits, live calls or GitHub changes.

## Targeted recheck

Historical partition is corrected to51/219/20. All27 source-artifact references now resolve; counts are4 P1/20 P2/3 P3. Bounded proof directories and external skill provenance are present. Remaining portability issue is an unstated mixed reference base: `coverage/evidence-coverage.json` report and several acceptance evidence references only resolve under `lanes/`. Normalize these or declare deterministic packet-root/lanes/frozen-source resolution. No product rereview was performed.

## Final correction recheck

Both packet findings are **fixed and verified**. `reference-resolution.json` explicitly defines packet, nested, frozen source, website and external-skill bases. The two acceptance references now name existing packet files, and the evidence coverage report path resolves from its containing directory. Historical51/219/20 partition remains correct. This closes only the previously identified packet corrections; it is not release approval or the subsequent exact-commit review.
