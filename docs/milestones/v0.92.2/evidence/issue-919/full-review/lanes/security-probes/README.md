Extracted exact run_process, apply_minimal_child_environment, process_output, truncate and redact_secret implementations from candidate adapters/mod.rs. Stub invocation/output types only. Synthetic config via wrapper after environment clearing, matching existing source regression technique. file:// only; no credential reads or network. Two cases and default-config -q negative control. This proves subprocess boundary behavior, not an end-to-end GitHub mutation.
