# DEMOS / INSTALLED JOURNEYS — bounded evidence review

Candidate `5c4a6149771c637f3c805985b86231077965eab4`. **DEMOS-001 P3:** PDF_EXPORT.md:46 calls a Cargo-built CLI test an installed lane. The test unconditionally uses CARGO_BIN_EXE_adl at codefriend_render_pdf.rs:183; rename the proof classification or provide an actual installed route. No new actionable renderer defect was found in the examined retained outputs. This is a completed review of the declared demo evidence boundaries, not a PASS for independent product qualification or release acceptance.

## What was actually inspected and reproduced

The complete16-row demo matrix was reconciled against source test evidence, retained installed-component records and publication gates. `demos-coverage.json` preserves each row, including complementary deployment/docs/history ownership. `demos-acceptance.json` maps40 relevant CF approval, output, integration and qualification criteria; no criterion is promoted from a fixture or historical binary to current installed acceptance.

All six retained report bytes match their native BLAKE3 manifest digests: three Markdown revisions for896, HTML897, and both PDF898 revisions. The native digest function is `adl/src/codefriend/ingestion/mod.rs:132`; SHA256 is recorded separately as transport identity and is not confused with the native digest. Full Markdown r3 and HTML semantic output were read, including evidence citations, attribution, action/test plans and output boundaries. HTML contains same-document navigation and an evidence index; no fresh browser accessibility acceptance is claimed.

Both retained PDFs were freshly extracted and rasterized. All ten retained page images were independently viewed. The original set matches at110dpi and the corrected Markdown-semantics set at120dpi; every retained PNG byte matches the corresponding fresh render. Five pages per document match each manifest. Extracted content matches after whitespace normalization. The corrected set preserves Unicode, long URL/wide-token wrapping, literal backslashes and readable paths; no clipped text, overlaps or missing final boundary were observed. The old set retains literal Markdown escapes as historical pre-remediation evidence; it is not reported as a current defect. Reproduction details are in `demos-artifacts/verification.json`.

## Installed and human-approval boundaries

Retained879/882–889 records name macOS binary hashes, installation provenance and bounded fixture scenarios. Those are historical installed-component records; their original binary bytes are not in the selected packet and were not revalidated. The current source tests invoke source-built binaries, except explicitly relocated component tests, and do not become installed-release proof because a test name says installed.888 includes earlier hosted Linux fixture evidence with explicit original failure exit codes; expected-negative job success does not mean passing policy assessment.

The897 HTML and898 PDF artifacts visibly say `operator-fixture`;896 Markdown r3 says `codex/root`. Their approval/binding integrity supports renderer behavior, not a new human approval.1109 records actual provider generation/retrieval for two partial architecture packages, with distinct candidate/source identities and explicit nonclaims; it does not complete915 ADL/external review or the human-approved export journey.

The916 record retains154 passing native tests across11targets plus141 website tests at earlier exact sources and explicitly says915 pending. The lead independently reran the frozen website suite141/141 in this review; that is source-component support.918 remains a draft with no selected accepted final binary/export package. D9/D10 and full914/915 installed/operator acceptance therefore remain explicitly pending. This is an already declared gate, not a new absence-based product finding.

## Independence and limits

The tests specialist performed this separate gap-analysis lane and did not author these product changes or retained renders. The same reviewer authored tests and demos reports; two report names do not represent two independent reviewers. No provider, deployed Runtime, cloud, shared-binary replacement or GitHub mutation was performed. D12 deployment, D13 ADR and D14/D15 predecessor acceptance rely on the separately owned deployment/docs/history lanes rather than unsupported claims here.

## Export/proof documentation coverage

All13 `docs/codefriend/*PROOF*.json` surfaces and the three export guides were reviewed. Canonical records were read fully; four historical variants were inspected via complete diffs against already-read records. Exact paths/ranges and observations are in `demos-coverage.json` under `authored_documentation_scope`. Historical supersession and macOS-only/non-provider limits are retained. No original installed binary was reconstructed or silently treated as current.
