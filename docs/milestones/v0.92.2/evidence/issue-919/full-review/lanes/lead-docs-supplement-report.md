# Lead documentation supplement — #919

**P3 DOC-SUP-001: current parser instructions describe obsolete limits.** `docs/codefriend/ARCHITECTURE.md:74` and `LOCAL_FITNESS.md:37` tell operators that Rust files are bounded to 32 KiB and 128 raw lexical units, including comments and literals. Frozen `rust_parse.rs:10` admits 400 KiB, 32768 tokens, nesting depth 32 and cumulative path units 2048; comments/literals are opaque. The current `RUST_PARSER_BOUNDS.md` accurately describes this contract and worker resource bounds. Link the operational guides to that contract so users do not needlessly exclude useful input or misdiagnose a budget error. Do not rewrite old proof records to imply that they executed the newer parser.

Candidate: `5c4a6149771c637f3c805985b86231077965eab4`. The per-path ledger is authoritative for review depth. This supplement covers all 40 changed v0.93 planning paths, 10 full CodeFriend guides, 14 CodeFriend structured proof/scope records, and the two targeted parser paragraphs. The lead separately reviewed the remaining CodeFriend guides. This is the code/architecture specialist's documentation supplement, not a new independent reviewer.

The v0.93 review found no additional actionable planning defect. All 64 work packages were inspected through their unique semantic fields; graph/spec rows agree, wave projections agree, every dependency resolves and the graph is acyclic. Routing preserves the public ADL/six private repository boundary, separate CodeFriend software/site ownership, migration bootstrap, RD-11 feature gate, mandatory Runtime v4 and CodeFriend launch tracks, and explicit opening approval after accepted v0.92.2 closeout. Future work is planned rather than falsely reported implemented. Existing Beta1 obligations remain current rather than being silently deferred.

Historical planning review inventories and CodeFriend proof records retain their bounded claims. All 24 saved template results are internally consistent and explicitly say structural validation only. The 72 object/evidence rows across six historical proof packets have structurally valid relative paths and source/digest metadata. This is provenance/claim classification, not original-byte verification or rerunning those proofs. Private TBD and Drive inputs were not reopened; the review checks the tracked allocations and successor routing, not the inaccessible original documents. All changed Markdown hunks and relevant surrounding context were reviewed; unchanged baseline text outside those contexts was not exhaustively re-reviewed.

No product, GitHub, runtime or cloud state was changed. Historical proof passes are not fresh candidate execution proof or milestone acceptance.
