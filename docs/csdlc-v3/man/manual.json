{
  "schema": "csdlc.operator_manual.v1",
  "pages": [
    {
      "name": "csdlc",
      "summary": "operate the authenticated C-SDLC v3 lifecycle",
      "sections": {
        "SYNOPSIS": [
          {
            "code": "csdlc --help\ncsdlc --contract\ncsdlc COMMAND --describe\ncsdlc COMMAND --help\nman csdlc-COMMAND\nman csdlc-requests\nman csdlc-workflow"
          }
        ],
        "DESCRIPTION": [
          "C-SDLC v3 is the current lifecycle authority after #505 / PR #591 only when canonical origin/main selector, authenticated native receipt, terminal reconciliation and Git objects validate. Missing or stale proof suspends authority. A printed plan, a construction report, an install, or a boolean in a request cannot grant it.",
          "This suite documents the committed native command contract. It includes all help-listed commands plus the supported rollback alias and github-issue create/close forms. The active simplification issues may change future behavior; rerun command/option parity after integration. Historical v1 wrappers and retained v2 binaries are not routine operational routes.",
          "Discovery is compiled from the current command manifest: --contract returns the raw full descriptor manifest and COMMAND --describe returns the raw command descriptor, including input contract, effect class and result schema. These global discovery forms do not execute the route and are separate from owner-specific parser options. The selected binary must support this contract; source or isolated candidate validation does not prove that the active installation has been upgraded.",
          "Semantic issue intent forms are the current operational contract. Direct issue-writer --request forms are retired and remain available only through --help and --describe for migration discovery. Observation and administrative request forms retain their documented contracts. See docs/csdlc-v3/INTENT_COMMANDS.md for strict content and effect boundaries."
        ],
        "INSTALLATION": [
          {
            "code": "bash adl/tools/install_owner_binaries.sh --bin csdlc\nexport PATH=\"$PWD/.adl/bin/native-v3:$PATH\"\nexport MANPATH=\"$PWD/.adl/bin/native-v3/share/man:${MANPATH:-}\"\nman csdlc\nman csdlc-finish"
          },
          "The owner installer installs the manual under STABLE_BIN_DIR/share/man/man1 and prints discovery instructions; it does not edit shell startup files or global system directories. Set PATH and MANPATH in your own shell as above. The trailing MANPATH colon retains system manual paths on macOS and Linux. --stable-bin-dir changes the binary and manual prefix together. A docs-only refresh does not rebuild or replace an unchanged binary.",
          "The native csdlc install command is an issue-bound proof/install route with a separate .adl/bin/csdlc destination and explicit approval. It is not the owner installer or manual installer. See csdlc-install(1)."
        ],
        "LIFECYCLE": [
          "Candidate ordinary sequence: status ISSUE; prepare ISSUE --plan PLAN; bind ISSUE; edit ISSUE --changes CHANGES; rebuild ISSUE; validate ISSUE; proof ISSUE; review ISSUE --evidence REVIEW; publish ISSUE; finish ISSUE; clean ISSUE. Recover ISSUE previews one retained native local transaction or remote operation for exact-token recovery. The existing request-based sequence follows below and remains supported; no activation is implied.",
          "Prepare issue-specific SIP -> STP -> SPP -> VPP -> SRP -> SOR using the active template registry. Native issue/edit/validate/doctor prepare a local issue; bind creates the registered execution worktree. Implement and validate proportionally, obtain independent exact-head review, and check native review before publication. Native publish validates publication admission; github-pr executes the actual create/update/ready/merge transport. Finish observes merged PR plus closed issue and may persist terminal records; clean independently previews and removes only the exact eligible registered checkout.",
          "Use csdlc-workflow(1) for the complete operator sequence. A successful command exit is not sufficient: inspect operational_authority, result status/findings, exact issue/head and mutation flags. A ready model-only plan is not executed work."
        ],
        "CONVENTIONS": [
          "--help and -h are standalone help arguments, not global options mixed into another invocation. There is no global --version, --json, --quiet, --root or --repo option. Candidate issue intent forms separately accept --json, --repo-root, --emit-request and --intent-request; consult their specific input contract. JSON is the machine output format; do not use retired wrapper flags. Paths to CLI input files are resolved by the invoking process. Typed relative path fields use the root declared by their owning contract; consult csdlc-requests(1).",
          "Local commands accept --repo-root for discovery and --v3-state-root for retained construction storage. Ordinary operations should run from clean primary main during preparation, then from the native-bound issue checkout. Do not use alternate roots to bypass ownership. Exact SHA means the full 40-hex Git revision. Local lifecycle digests and remote canonical selector digests are different values.",
          "Machine command results preserve legacy owner payload fields and add /envelope with schema csdlc.v3.command_result.v1. Successful --contract and COMMAND --describe responses return their descriptor directly; help remains text. Process outcome, lifecycle status, authority and effects are separate. A successful exit or ready report does not prove mutation, publication or terminal delivery. Unknown effects and unreported evidence invalidation are not no-effect evidence; inspect owner results and durable receipts."
        ],
        "CREDENTIALS": [
          "Select credential names, never credential values, in credential_names or --credential-name. The default simple-issue name is GITHUB_TOKEN. The current native resolver first reads the selected environment variable; if absent, GITHUB_TOKEN or GH_TOKEN may use the explicitly set ADL_GITHUB_TOKEN_FILE. A set-but-empty variable fails; it does not fall back. Other credential names require their corresponding environment variable.",
          "For the operator-approved token file use ADL_GITHUB_TOKEN_FILE=\"$HOME/keys/github.token\" only on commands needing GitHub access. Do not print or retain its contents. This committed native resolver requires the explicit file variable; it does not automatically discover a default file. Invalid credentials, unsafe token characters or unavailable authentication fail closed. Child processes receive scoped credentials and sanitized environments. Read-only curl transport supplies its private configuration over stdin and disables ambient curl configuration with -q first. Mutating transport retains its separate private input/receipt effects; read-only observation does not create a credential configuration file."
        ],
        "EXIT STATUS": [
          "0 means the command returned a report (or help); some planning reports still contain blocked findings. 2 means usage, parsing, guard, transport or execution failure. Read the structured status as well as the process exit status.",
          "JSON reports, including machine-readable command failures, go to stdout with the common envelope. Failure diagnostics on stderr identify a stable reason and direct the operator to structured stdout; they do not duplicate JSON payloads. Help remains text and successful descriptor discovery returns raw descriptors. This manual does not claim ADL_OBSERVABILITY_STDERR or ADL_OBSERVABILITY_LOG compatibility beyond separately proven owner behavior."
        ],
        "FILES": [
          "Primary local preparation: resolved Git metadata csdlc-v3/local/issues/ISSUE/{index.json,values/,cards/}. In an ordinary primary checkout this is .git/csdlc-v3/local. Linked issue state: .csdlc/issues/ISSUE/{index.json,values/,cards/}. Local locks and transactions live beneath that resolved local state root, including transactions/ISSUE.json and completed transaction records.",
          "Remote intents, recovery and mutation receipts live beneath resolved common Git metadata csdlc-v3/remote/. Merge intent/reconciliation lives in remote/merges/. Temporary private transport inputs live under resolved Git runtime metadata. Do not delete uncertain intent, locks, journals, stages or backup records as a recovery shortcut.",
          "Terminal state is LOCAL_ROOT/v3/issues/ISSUE/terminal.json and LOCAL_ROOT/evidence/ISSUE/terminal-receipt.json. LOCAL_ROOT is resolved Git metadata csdlc-v3/local on primary and .csdlc in linked checkout. Use paths returned by the native result and preserve exact topology. Proof artifacts are issue-scoped .csdlc/evidence/ISSUE in the invoking bound checkout."
        ],
        "DIAGNOSTICS": [
          "authority missing/stale: inspect canonical origin/main selector, native receipt and referenced Git objects, then route authority repair through its owner. Do not switch to v2 or raw GitHub writes.",
          "stale_lifecycle_digest: reread actual native index/generation, compare the intended field edits, refresh the request from that state, and validate. Never copy an unrelated issue digest.",
          "github_credential_missing or credential resolution failed: set the approved credential source explicitly and retry the same read. Never put the token into JSON or argv.",
          "review stale/head mismatch: verify actual branch HEAD, rerun affected proof and independent review at that head, refresh the typed receipt and native review. Do not relabel an old receipt.",
          "incorrect checkout topology/invalid_operational_roots: inspect git status --short --branch and git worktree list --porcelain. Return to the exact registered branch and native-bound checkout; preserve existing work. Do not hand-edit index binding or register fake worktrees.",
          "finish without --observe-github: a caller-provided public_adapter_receipt is not authentication. Use native finish --observe-github with numeric PR, expected head and closing mode; a still-open issue or unmerged PR remains blocked.",
          "uncertain remote mutation/reconciliation: preserve request and intent bytes, then reissue the identical native request to reconcile authenticated remote state. Do not change IDs, delete receipts or repeat a raw write. Only explicitly supported creation/ready absence recovery may request its one-shot retry. Merge and issue-edit uncertain retries are observation-only.",
          "Doctor, validate, eligibility, schedule and shepherd observe without creating lifecycle locks or replaying interrupted transactions. Reported recovery-required state must be handled by an explicit mutation/recovery route. Operational local routes fail closed when authority context is unavailable; only explicit local selects the non-operational construction helper. This guarantee does not make issue, edit or bind observational."
        ],
        "SEE ALSO": [
          "csdlc-workflow(1), csdlc-requests(1), csdlc-doctor(1), csdlc-github-pr(1), csdlc-finish(1), csdlc-clean(1). Repository policy: AGENTS.md, docs/csdlc-v3/CURRENT_AUTHORITY.md, docs/csdlc-v3/CONTRACT.md and docs/csdlc-v3/PULL_REQUEST_MERGE.md.",
          "csdlc-status(1), csdlc-prepare(1), csdlc-rebuild(1), csdlc-recover(1), docs/csdlc-v3/INTENT_COMMANDS.md."
        ]
      }
    },
    {
      "name": "csdlc-local",
      "summary": "inspect retained local preparation model",
      "sections": {
        "SYNOPSIS": [
          {
            "code": "csdlc local --request REQUEST --registry REGISTRY --registrations REGISTRATIONS\n  [--repo-root ROOT] [--v3-state-root ROOT]\ncsdlc local --help"
          }
        ],
        "DESCRIPTION": [
          "Constructs render, dependency and bind plans from the request, active registry and registration inventory. It neither creates a GitHub issue nor binds a checkout. A proposed branch generally must have a matching registration for the retained helper plan; do not invent a registration to obtain a green result."
        ],
        "EFFECTS": [
          "Read-only helper; no operational authority.",
          "Do not interpret read_only in an older report as a byte-level filesystem guarantee. See csdlc(1) for current observation guarantees, selected-binary discovery and the primary/linked path distinction."
        ],
        "OPTIONS": [
          "--request: Required path to a UTF-8 JSON request of the command's request family.",
          "--registry: Required path to docs/templates/prompts/current.json; selects the active versioned templates and schemas.",
          "--registrations: Required JSON array of actual Git worktree registrations, each {branch, worktree, primary}; refresh from live git worktree list --porcelain.",
          "--repo-root: Explicit repository discovery root. Required by foundation/sprint; optional for local-family commands. It does not override authenticated binding guards.",
          "--v3-state-root: Optional retained construction state root for issue/eligibility; not a primary-checkout artifact location or an ownership bypass.",
          "--help: Show standalone help; -h is equivalent.",
          "-h: Show standalone help; do not combine with execution options."
        ],
        "REQUEST": [
          "LocalPreparationRequest; complete fields, commands and canonical locations are in csdlc-requests(1). Example source: docs/csdlc-v3/man/examples/local.json. Replace example identity with the actual issue, approved branch/worktree, active registry version and latest lifecycle digest before operational use."
        ],
        "EXAMPLES": [
          {
            "code": "csdlc local --request local-request.json \\\n  --registry docs/templates/prompts/current.json \\\n  --registrations actual-registrations.json"
          },
          "The checked-in example is fixture-validated. A JSON-valid example does not grant an operational binding or assert dependency readiness. Use csdlc-workflow(1) to prepare and refresh these inputs from real state."
        ],
        "RECOVERY": [
          "Preserve index, values and transaction bytes. Reissue the exact interrupted mutating issue/bind/edit request through its native owner after checking the bound topology; do not use diagnostics as a repair command. If current recovery is denied, retain the finding and route a bounded tooling repair. Never hand-edit transaction stages/backups or stale digest guards."
        ],
        "SEE ALSO": [
          "csdlc(1), csdlc-requests(1), csdlc-workflow(1)."
        ]
      }
    },
    {
      "name": "csdlc-issue",
      "summary": "retired preparation writer; use prepare intent",
      "sections": {
        "SYNOPSIS": [
          {
            "code": "csdlc prepare ISSUE --plan PLAN.json\ncsdlc prepare --intent-request REQUEST.json\ncsdlc issue --help"
          }
        ],
        "DESCRIPTION": [
          "The direct issue --request writer is retired and fails before request parsing or effects. Use prepare with an issue number and reviewed plan, or an exact generated --intent-request. The issue command remains discoverable through --help and --describe for migration."
        ],
        "EFFECTS": [
          "Discovery is read-only. Current preparation effects belong to the semantic prepare intent and its native transaction owner."
        ],
        "OPTIONS": [
          "--plan: Required semantic plan JSON for ordinary prepare.",
          "--intent-request: Exact generated semantic request or wrapper.",
          "--request, --registry, --registrations, --v3-state-root: Retired writer inputs documented only for migration discovery.",
          "--repo-root: Select repository discovery for the semantic intent.",
          "--help: Show migration guidance; -h is equivalent."
        ],
        "REQUEST": [
          "Use csdlc.v3.intent_request.v1 for advanced execution. LocalPreparationRequest is a retired direct-writer input and grants no CLI mutation authority."
        ],
        "EXAMPLES": [
          {
            "code": "csdlc prepare 870 --plan plan.json"
          }
        ],
        "RECOVERY": [
          "Preserve index, values and transaction bytes. Reissue the exact interrupted mutating issue/bind/edit request through its native owner after checking the bound topology; do not use diagnostics as a repair command. If current recovery is denied, retain the finding and route a bounded tooling repair. Never hand-edit transaction stages/backups or stale digest guards."
        ],
        "SEE ALSO": [
          "csdlc(1), csdlc-requests(1), csdlc-workflow(1)."
        ]
      }
    },
    {
      "name": "csdlc-bind",
      "summary": "bind an issue to its registered execution checkout",
      "sections": {
        "SYNOPSIS": [
          {
            "code": "csdlc bind ISSUE\ncsdlc bind --intent-request REQUEST.json\ncsdlc bind --help"
          }
        ],
        "ISSUE INTENT FORM": [
          "The semantic issue intent form is the current operational interface. It resolves issue identity and routes effects through the native semantic transaction owner.",
          "Bind the prepared issue through the existing registered-worktree owner; derive registry and registration inputs from canonical context.",
          "Optional --repo-root selects discovery, --json retains machine output, and --emit-request returns a generated wrapper accepted directly by COMMAND --intent-request REQUEST.json; its inner request is also accepted. Both forms share identity and mutation guards."
        ],
        "DESCRIPTION": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "Requires ready issue-specific cards, current digest, authenticated canonical authority and the tracked .adl/worktree-policy.json approved parent. Creates the exact branch and FastWork checkout when absent, or validates supported existing binding. Do not manually create a competing checkout. Verify bound phase, returned digest and all six cards before implementation."
        ],
        "EFFECTS": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "Mutates Git branch/worktree and local issue state.",
          "Do not interpret read_only in an older report as a byte-level filesystem guarantee. See csdlc(1) for current observation guarantees, selected-binary discovery and the primary/linked path distinction."
        ],
        "OPTIONS": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "--request: Required path to a UTF-8 JSON request of the command's request family.",
          "--registry: Required path to docs/templates/prompts/current.json; selects the active versioned templates and schemas.",
          "--registrations: Required JSON array of actual Git worktree registrations, each {branch, worktree, primary}; refresh from live git worktree list --porcelain.",
          "--repo-root: Explicit repository discovery root. Required by foundation/sprint; optional for local-family commands. It does not override authenticated binding guards.",
          "--v3-state-root: Optional retained construction state root for issue/eligibility; not a primary-checkout artifact location or an ownership bypass.",
          "--help: Show standalone help; -h is equivalent.",
          "-h: Show standalone help; do not combine with execution options.",
          "--intent-request: Execute an exact generated csdlc.v3.intent_request.v1 request or its generated wrapper; stale snapshots fail closed."
        ],
        "REQUEST": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "LocalPreparationRequest; complete fields, commands and canonical locations are in csdlc-requests(1). Example source: docs/csdlc-v3/man/examples/local.json. Replace example identity with the actual issue, approved branch/worktree, active registry version and latest lifecycle digest before operational use."
        ],
        "EXAMPLES": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          {
            "code": "csdlc bind 870"
          },
          "The checked-in example is fixture-validated. A JSON-valid example does not grant an operational binding or assert dependency readiness. Use csdlc-workflow(1) to prepare and refresh these inputs from real state."
        ],
        "RECOVERY": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "Preserve index, values and transaction bytes. Reissue the exact interrupted mutating issue/bind/edit request through its native owner after checking the bound topology; do not use diagnostics as a repair command. If current recovery is denied, retain the finding and route a bounded tooling repair. Never hand-edit transaction stages/backups or stale digest guards."
        ],
        "SEE ALSO": [
          "csdlc(1), csdlc-requests(1), csdlc-workflow(1)."
        ]
      }
    },
    {
      "name": "csdlc-edit",
      "summary": "edit declared card values transactionally",
      "sections": {
        "SYNOPSIS": [
          {
            "code": "csdlc edit ISSUE --changes CHANGES.json\ncsdlc edit --intent-request REQUEST.json\ncsdlc edit --help"
          }
        ],
        "ISSUE INTENT FORM": [
          "The semantic issue intent form is the current operational interface. It resolves issue identity and routes effects through the native semantic transaction owner.",
          "Apply strict csdlc.v3.intent_changes.v1 content with schema, cards and a mandatory amendment declaration through the native editor. The declaration selects scope_acceptance, plan, proof_validator, implementation, review or display_only; binding remains reserved for the topology-verified bind owner. Unsupported preview/execute flags fail before dispatch.",
          "Optional --repo-root selects discovery, --json retains machine output, and --emit-request returns a generated wrapper accepted directly by COMMAND --intent-request REQUEST.json; its inner request is also accepted. Both forms share identity and mutation guards."
        ],
        "DESCRIPTION": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "card_updates maps sip/stp/spp/vpp/srp/sor to semantic field values. The native renderer validates locked template shape and commits the transaction. Use the appropriate editor skill to determine truthful fields; never patch generated Markdown. Compare-and-swap expected_lifecycle_digest binds the existing issue generation."
        ],
        "EFFECTS": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "Mutates issue values, renders, index and transaction records.",
          "Do not interpret read_only in an older report as a byte-level filesystem guarantee. See csdlc(1) for current observation guarantees, selected-binary discovery and the primary/linked path distinction."
        ],
        "OPTIONS": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "--request: Required path to a UTF-8 JSON request of the command's request family.",
          "--registry: Required path to docs/templates/prompts/current.json; selects the active versioned templates and schemas.",
          "--registrations: Required JSON array of actual Git worktree registrations, each {branch, worktree, primary}; refresh from live git worktree list --porcelain.",
          "--repo-root: Explicit repository discovery root. Required by foundation/sprint; optional for local-family commands. It does not override authenticated binding guards.",
          "--v3-state-root: Optional retained construction state root for issue/eligibility; not a primary-checkout artifact location or an ownership bypass.",
          "--help: Show standalone help; -h is equivalent.",
          "-h: Show standalone help; do not combine with execution options.",
          "--intent-request: Execute an exact generated csdlc.v3.intent_request.v1 request or its generated wrapper; stale snapshots fail closed."
        ],
        "REQUEST": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "LocalPreparationRequest; complete fields, commands and canonical locations are in csdlc-requests(1). Example source: docs/csdlc-v3/man/examples/local.json. Replace example identity with the actual issue, approved branch/worktree, active registry version and latest lifecycle digest before operational use."
        ],
        "EXAMPLES": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          {
            "code": "csdlc edit 870 --changes card-changes.json"
          },
          "The checked-in example is fixture-validated. A JSON-valid example does not grant an operational binding or assert dependency readiness. Use csdlc-workflow(1) to prepare and refresh these inputs from real state."
        ],
        "RECOVERY": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "Preserve index, values and transaction bytes. Reissue the exact interrupted mutating issue/bind/edit request through its native owner after checking the bound topology; do not use diagnostics as a repair command. If current recovery is denied, retain the finding and route a bounded tooling repair. Never hand-edit transaction stages/backups or stale digest guards."
        ],
        "SEE ALSO": [
          "csdlc(1), csdlc-requests(1), csdlc-workflow(1)."
        ]
      }
    },
    {
      "name": "csdlc-validate",
      "summary": "validate the six-card issue bundle",
      "sections": {
        "SYNOPSIS": [
          {
            "code": "csdlc validate ISSUE"
          },
          {
            "code": "csdlc validate --request REQUEST --registry REGISTRY --registrations REGISTRATIONS\n  [--repo-root ROOT] [--v3-state-root ROOT]\ncsdlc validate --help"
          }
        ],
        "ISSUE INTENT FORM": [
          "Semantic issue intent forms are the current operational contract. Direct issue-writer --request forms are retired and remain available only through --help and --describe for migration discovery. Observation and administrative request forms retain their documented contracts. See docs/csdlc-v3/INTENT_COMMANDS.md for strict content and effect boundaries.",
          "Observe native card validation without executing validators or replaying interrupted transactions.",
          "Optional --repo-root selects discovery, --json retains machine output, and --emit-request returns a generated wrapper accepted directly by COMMAND --intent-request REQUEST.json; its inner request is also accepted. Both forms share identity and mutation guards."
        ],
        "DESCRIPTION": [
          "The following details describe the retained request-based observation form. The current semantic issue form is specified above.",
          "Checks values, rendered structures and index digest against the active registry. A missing or malformed bundle blocks; this is structural validation, not implementation or semantic proof. The route uses the request's plan_pvf capability mapping, not a fictional validate command enum."
        ],
        "EFFECTS": [
          "The following details describe the retained request-based observation form. The current semantic issue form is specified above.",
          "Observation-only diagnostic route; does not create lifecycle lock metadata or replay a pending transaction. Pending transaction recovery is reported as local_transaction_recovery_required and must use an explicit mutation/recovery route.",
          "Do not interpret read_only in an older report as a byte-level filesystem guarantee. See csdlc(1) for current observation guarantees, selected-binary discovery and the primary/linked path distinction."
        ],
        "OPTIONS": [
          "The following details describe the retained request-based observation form. The current semantic issue form is specified above.",
          "--request: Required path to a UTF-8 JSON request of the command's request family.",
          "--registry: Required path to docs/templates/prompts/current.json; selects the active versioned templates and schemas.",
          "--registrations: Required JSON array of actual Git worktree registrations, each {branch, worktree, primary}; refresh from live git worktree list --porcelain.",
          "--repo-root: Explicit repository discovery root. Required by foundation/sprint; optional for local-family commands. It does not override authenticated binding guards.",
          "--v3-state-root: Optional retained construction state root for issue/eligibility; not a primary-checkout artifact location or an ownership bypass.",
          "--help: Show standalone help; -h is equivalent.",
          "-h: Show standalone help; do not combine with execution options."
        ],
        "REQUEST": [
          "The following details describe the retained request-based observation form. The current semantic issue form is specified above.",
          "LocalPreparationRequest; complete fields, commands and canonical locations are in csdlc-requests(1). Example source: docs/csdlc-v3/man/examples/local.json. Replace example identity with the actual issue, approved branch/worktree, active registry version and latest lifecycle digest before operational use."
        ],
        "EXAMPLES": [
          "The following details describe the retained request-based observation form. The current semantic issue form is specified above.",
          {
            "code": "csdlc validate --request local-request.json \\\n  --registry docs/templates/prompts/current.json \\\n  --registrations actual-registrations.json"
          },
          "The checked-in example is fixture-validated. A JSON-valid example does not grant an operational binding or assert dependency readiness. Use csdlc-workflow(1) to prepare and refresh these inputs from real state."
        ],
        "RECOVERY": [
          "The following details describe the retained request-based observation form. The current semantic issue form is specified above.",
          "Preserve index, values and transaction bytes. Reissue the exact interrupted mutating issue/bind/edit request through its native owner after checking the bound topology; do not use diagnostics as a repair command. If current recovery is denied, retain the finding and route a bounded tooling repair. Never hand-edit transaction stages/backups or stale digest guards."
        ],
        "SEE ALSO": [
          "csdlc(1), csdlc-requests(1), csdlc-workflow(1)."
        ]
      }
    },
    {
      "name": "csdlc-doctor",
      "summary": "diagnose local lifecycle readiness",
      "sections": {
        "SYNOPSIS": [
          {
            "code": "csdlc doctor --request REQUEST --registry REGISTRY --registrations REGISTRATIONS\n  [--repo-root ROOT] [--v3-state-root ROOT]\ncsdlc doctor --help"
          }
        ],
        "DESCRIPTION": [
          "Checks the native lifecycle and six cards, returns findings and next_route. Missing or invalid operational authority context fails closed; only explicit local selects non-authoritative construction inspection. Check operational_authority and full findings before considering binding. It does not establish independent implementation review."
        ],
        "EFFECTS": [
          "Observation-only diagnostic route; does not create lifecycle lock metadata or replay a pending transaction. Pending transaction recovery is reported as local_transaction_recovery_required and must use an explicit mutation/recovery route.",
          "Do not interpret read_only in an older report as a byte-level filesystem guarantee. See csdlc(1) for current observation guarantees, selected-binary discovery and the primary/linked path distinction."
        ],
        "OPTIONS": [
          "--request: Required path to a UTF-8 JSON request of the command's request family.",
          "--registry: Required path to docs/templates/prompts/current.json; selects the active versioned templates and schemas.",
          "--registrations: Required JSON array of actual Git worktree registrations, each {branch, worktree, primary}; refresh from live git worktree list --porcelain.",
          "--repo-root: Explicit repository discovery root. Required by foundation/sprint; optional for local-family commands. It does not override authenticated binding guards.",
          "--v3-state-root: Optional retained construction state root for issue/eligibility; not a primary-checkout artifact location or an ownership bypass.",
          "--help: Show standalone help; -h is equivalent.",
          "-h: Show standalone help; do not combine with execution options."
        ],
        "REQUEST": [
          "LocalPreparationRequest; complete fields, commands and canonical locations are in csdlc-requests(1). Example source: docs/csdlc-v3/man/examples/local.json. Replace example identity with the actual issue, approved branch/worktree, active registry version and latest lifecycle digest before operational use."
        ],
        "EXAMPLES": [
          {
            "code": "csdlc doctor --request local-request.json \\\n  --registry docs/templates/prompts/current.json \\\n  --registrations actual-registrations.json"
          },
          "The checked-in example is fixture-validated. A JSON-valid example does not grant an operational binding or assert dependency readiness. Use csdlc-workflow(1) to prepare and refresh these inputs from real state."
        ],
        "RECOVERY": [
          "Preserve index, values and transaction bytes. Reissue the exact interrupted mutating issue/bind/edit request through its native owner after checking the bound topology; do not use diagnostics as a repair command. If current recovery is denied, retain the finding and route a bounded tooling repair. Never hand-edit transaction stages/backups or stale digest guards."
        ],
        "SEE ALSO": [
          "csdlc(1), csdlc-requests(1), csdlc-workflow(1)."
        ]
      }
    },
    {
      "name": "csdlc-eligibility",
      "summary": "inspect lifecycle eligibility",
      "sections": {
        "SYNOPSIS": [
          {
            "code": "csdlc eligibility --request REQUEST --registry REGISTRY --registrations REGISTRATIONS\n  [--repo-root ROOT] [--v3-state-root ROOT]\ncsdlc eligibility --help"
          }
        ],
        "DESCRIPTION": [
          "Inspects native issue state and eligibility under current authority. Operational context discovery fails closed instead of selecting a construction report; use explicit local only for historical construction inspection. Eligibility is not permission to execute a different issue or override dependency gates."
        ],
        "EFFECTS": [
          "Observation-only diagnostic route; does not create lifecycle lock metadata or replay a pending transaction. Pending transaction recovery is reported as local_transaction_recovery_required and must use an explicit mutation/recovery route.",
          "Do not interpret read_only in an older report as a byte-level filesystem guarantee. See csdlc(1) for current observation guarantees, selected-binary discovery and the primary/linked path distinction."
        ],
        "OPTIONS": [
          "--request: Required path to a UTF-8 JSON request of the command's request family.",
          "--registry: Required path to docs/templates/prompts/current.json; selects the active versioned templates and schemas.",
          "--registrations: Required JSON array of actual Git worktree registrations, each {branch, worktree, primary}; refresh from live git worktree list --porcelain.",
          "--repo-root: Explicit repository discovery root. Required by foundation/sprint; optional for local-family commands. It does not override authenticated binding guards.",
          "--v3-state-root: Optional retained construction state root for issue/eligibility; not a primary-checkout artifact location or an ownership bypass.",
          "--help: Show standalone help; -h is equivalent.",
          "-h: Show standalone help; do not combine with execution options."
        ],
        "REQUEST": [
          "LocalPreparationRequest; complete fields, commands and canonical locations are in csdlc-requests(1). Example source: docs/csdlc-v3/man/examples/local.json. Replace example identity with the actual issue, approved branch/worktree, active registry version and latest lifecycle digest before operational use."
        ],
        "EXAMPLES": [
          {
            "code": "csdlc eligibility --request local-request.json \\\n  --registry docs/templates/prompts/current.json \\\n  --registrations actual-registrations.json"
          },
          "The checked-in example is fixture-validated. A JSON-valid example does not grant an operational binding or assert dependency readiness. Use csdlc-workflow(1) to prepare and refresh these inputs from real state."
        ],
        "RECOVERY": [
          "Preserve index, values and transaction bytes. Reissue the exact interrupted mutating issue/bind/edit request through its native owner after checking the bound topology; do not use diagnostics as a repair command. If current recovery is denied, retain the finding and route a bounded tooling repair. Never hand-edit transaction stages/backups or stale digest guards."
        ],
        "SEE ALSO": [
          "csdlc(1), csdlc-requests(1), csdlc-workflow(1)."
        ]
      }
    },
    {
      "name": "csdlc-schedule",
      "summary": "check six-dimensional issue readiness",
      "sections": {
        "SYNOPSIS": [
          {
            "code": "csdlc schedule --request REQUEST --registry REGISTRY --registrations REGISTRATIONS\n  [--repo-root ROOT] [--v3-state-root ROOT]\ncsdlc schedule --help"
          }
        ],
        "DESCRIPTION": [
          "Requires schedule_readiness with phase_ready, cards_ready, design_ready, dependencies_ready, paths_clear and budget_available. All six must be truthfully supported. Reports eligible operations and blockers; it does not launch children or remove dependency edges."
        ],
        "EFFECTS": [
          "Observation-only diagnostic route; does not create lifecycle lock metadata or replay a pending transaction. Pending transaction recovery is reported as local_transaction_recovery_required and must use an explicit mutation/recovery route.",
          "Do not interpret read_only in an older report as a byte-level filesystem guarantee. See csdlc(1) for current observation guarantees, selected-binary discovery and the primary/linked path distinction."
        ],
        "OPTIONS": [
          "--request: Required path to a UTF-8 JSON request of the command's request family.",
          "--registry: Required path to docs/templates/prompts/current.json; selects the active versioned templates and schemas.",
          "--registrations: Required JSON array of actual Git worktree registrations, each {branch, worktree, primary}; refresh from live git worktree list --porcelain.",
          "--repo-root: Explicit repository discovery root. Required by foundation/sprint; optional for local-family commands. It does not override authenticated binding guards.",
          "--v3-state-root: Optional retained construction state root for issue/eligibility; not a primary-checkout artifact location or an ownership bypass.",
          "--help: Show standalone help; -h is equivalent.",
          "-h: Show standalone help; do not combine with execution options."
        ],
        "REQUEST": [
          "LocalPreparationRequest; complete fields, commands and canonical locations are in csdlc-requests(1). Example source: docs/csdlc-v3/man/examples/local.json. Replace example identity with the actual issue, approved branch/worktree, active registry version and latest lifecycle digest before operational use."
        ],
        "EXAMPLES": [
          {
            "code": "csdlc schedule --request local-request.json \\\n  --registry docs/templates/prompts/current.json \\\n  --registrations actual-registrations.json"
          },
          "The checked-in example is fixture-validated. A JSON-valid example does not grant an operational binding or assert dependency readiness. Use csdlc-workflow(1) to prepare and refresh these inputs from real state."
        ],
        "RECOVERY": [
          "Preserve index, values and transaction bytes. Reissue the exact interrupted mutating issue/bind/edit request through its native owner after checking the bound topology; do not use diagnostics as a repair command. If current recovery is denied, retain the finding and route a bounded tooling repair. Never hand-edit transaction stages/backups or stale digest guards."
        ],
        "SEE ALSO": [
          "csdlc(1), csdlc-requests(1), csdlc-workflow(1)."
        ]
      }
    },
    {
      "name": "csdlc-shepherd",
      "summary": "select the next issue route from explicit state",
      "sections": {
        "SYNOPSIS": [
          {
            "code": "csdlc shepherd --request REQUEST --registry REGISTRY --registrations REGISTRATIONS\n  [--repo-root ROOT] [--v3-state-root ROOT]\ncsdlc shepherd --help"
          }
        ],
        "DESCRIPTION": [
          "Consumes shepherd_routing validation/dependency_wait/retryable_failure/repair_needed/operator_decision_needed and native phase. Returns routing state and eligible operations. It does not create workers, dispatch tasks or perform the suggested next operation."
        ],
        "EFFECTS": [
          "Observation-only diagnostic route; does not create lifecycle lock metadata or replay a pending transaction. Pending transaction recovery is reported as local_transaction_recovery_required and must use an explicit mutation/recovery route.",
          "Do not interpret read_only in an older report as a byte-level filesystem guarantee. See csdlc(1) for current observation guarantees, selected-binary discovery and the primary/linked path distinction."
        ],
        "OPTIONS": [
          "--request: Required path to a UTF-8 JSON request of the command's request family.",
          "--registry: Required path to docs/templates/prompts/current.json; selects the active versioned templates and schemas.",
          "--registrations: Required JSON array of actual Git worktree registrations, each {branch, worktree, primary}; refresh from live git worktree list --porcelain.",
          "--repo-root: Explicit repository discovery root. Required by foundation/sprint; optional for local-family commands. It does not override authenticated binding guards.",
          "--v3-state-root: Optional retained construction state root for issue/eligibility; not a primary-checkout artifact location or an ownership bypass.",
          "--help: Show standalone help; -h is equivalent.",
          "-h: Show standalone help; do not combine with execution options."
        ],
        "REQUEST": [
          "LocalPreparationRequest; complete fields, commands and canonical locations are in csdlc-requests(1). Example source: docs/csdlc-v3/man/examples/local.json. Replace example identity with the actual issue, approved branch/worktree, active registry version and latest lifecycle digest before operational use."
        ],
        "EXAMPLES": [
          {
            "code": "csdlc shepherd --request local-request.json \\\n  --registry docs/templates/prompts/current.json \\\n  --registrations actual-registrations.json"
          },
          "The checked-in example is fixture-validated. A JSON-valid example does not grant an operational binding or assert dependency readiness. Use csdlc-workflow(1) to prepare and refresh these inputs from real state."
        ],
        "RECOVERY": [
          "Preserve index, values and transaction bytes. Reissue the exact interrupted mutating issue/bind/edit request through its native owner after checking the bound topology; do not use diagnostics as a repair command. If current recovery is denied, retain the finding and route a bounded tooling repair. Never hand-edit transaction stages/backups or stale digest guards."
        ],
        "SEE ALSO": [
          "csdlc(1), csdlc-requests(1), csdlc-workflow(1)."
        ]
      }
    },
    {
      "name": "csdlc-github",
      "summary": "retired generic mutation writer; use issue-scoped GitHub intents",
      "sections": {
        "SYNOPSIS": [
          {
            "code": "csdlc github-issue ISSUE --operation OPERATION.json [--execute]\ncsdlc github-pr ISSUE --operation OPERATION.json [--execute]\ncsdlc github --help"
          }
        ],
        "DESCRIPTION": [
          "The generic github --request writer is retired and fails before request parsing or transport. Use the issue-scoped github-issue or github-pr semantic intent. The github command remains discoverable through --help and --describe for migration."
        ],
        "OPTIONS": [
          "--operation: Reviewed issue or pull-request operation content for the semantic owner.",
          "--execute: Execute an admitted github-issue or github-pr operation.",
          "--request and --observe-github: Retired generic-writer inputs documented only for migration discovery.",
          "--help: Show migration guidance; -h is equivalent."
        ],
        "REQUEST": [
          "Use the issue-scoped semantic operation or an exact generated --intent-request. OperationalRemoteDispatchRequest is a retired direct-writer input."
        ],
        "EFFECTS": [
          "Discovery is read-only. Remote effects belong to github-issue and github-pr semantic transactions."
        ],
        "EXAMPLES": [
          {
            "code": "csdlc github-issue 870 --operation issue-operation.json --execute"
          }
        ],
        "RECOVERY": [
          "After uncertain dispatch retain the original request and exact intent. Reinvoke the identical native request for authenticated reconciliation. A changed request is a different operation, not a retry. Never delete the target guard or intent to force a second write.",
          "Issue-edit does not support write replay, including retry_after_authenticated_absence; reconcile exact retained target and preserve later human changes. Supported create/ready absence retry is explicit, authenticated and one-shot. Merge retries are always read-only; a still-open or uncertain PR remains blocked."
        ],
        "SEE ALSO": [
          "csdlc(1), csdlc-requests(1), csdlc-workflow(1), csdlc-finish(1)."
        ]
      }
    },
    {
      "name": "csdlc-github-issue",
      "summary": "create, comment, edit or administratively close GitHub issues",
      "sections": {
        "SYNOPSIS": [
          {
            "code": "csdlc github-issue ISSUE --operation OPERATION.json [--execute]\ncsdlc github-issue --intent-request REQUEST.json\ncsdlc github-issue --help"
          }
        ],
        "ISSUE INTENT FORM": [
          "The semantic issue intent form is the current operational interface. It resolves issue identity and routes effects through the native semantic transaction owner.",
          "The strict operation content selects issue create/comment/edit/administrative close. Without --execute this previews native shape/identity admission. Creation uses the issue as coordination context and native issue zero for the newly assigned GitHub number. Explicit absence-retry content is {operation: action object, recovery: retry_after_authenticated_absence}; only native-supported retries are allowed.",
          "Optional --repo-root selects discovery, --json retains machine output, and --emit-request returns a generated wrapper accepted directly by COMMAND --intent-request REQUEST.json; its inner request is also accepted. Both forms share identity and mutation guards.",
          "The optional --preview explicitly selects the default observational intent mode and cannot be combined with --execute."
        ],
        "DESCRIPTION": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "With --execute owns only issue_create, issue_comment, issue_edit and issue_close. Creation returns the assigned GitHub number and durable receipt. Administrative duplicate/superseded/no-op closure is not delivery; completed closure is rejected. Use the simple forms documented below or the typed dispatch."
        ],
        "OPTIONS": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "--request: Required path to a UTF-8 JSON request of the command's request family.",
          "--observe-github: Perform authenticated GitHub readback. Requires selected credentials. It does not itself create, update or merge a PR.",
          "--execute: Select typed operational dispatch, not a generic force flag. Cannot combine with --observe-github on non-pr-state remote routes.",
          "--help: Show standalone help; -h is equivalent.",
          "-h: Show standalone help; do not combine with execution options.",
          "--intent-request: Execute an exact generated csdlc.v3.intent_request.v1 request or its generated wrapper; stale snapshots fail closed."
        ],
        "REQUEST": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "Without --execute: RemoteRouteRequest, including typed review/readback receipt references when required. With --execute on non-pr-state: OperationalRemoteDispatchRequest, whose expected_lifecycle_digest is the canonical authority selector payload digest, not the local issue index digest. See csdlc-requests(1).",
          "Non-pr-state --execute plus --observe-github is rejected. --observe-github without --execute uses authenticated readback in the planning path; it does not turn the returned plan into a performed mutation. Request family must match the selected mode."
        ],
        "EFFECTS": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "Readback/planning does not perform remote mutation. Operational github mutation writes durable intent before network dispatch and retains authenticated reconciliation/receipt. Successful review/publish plan reports are not PR creation. Credential names are scoped to the adapter; no raw token in argv or request."
        ],
        "EXAMPLES": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          {
            "code": "csdlc github-issue 870 --operation issue-operation.json --execute"
          },
          "Request fixtures are in docs/csdlc-v3/man/examples/; values are illustrative and must be bound to the actual head and reviewed issue. See csdlc-workflow(1) for the full live sequence."
        ],
        "RECOVERY": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "After uncertain dispatch retain the original request and exact intent. Reinvoke the identical native request for authenticated reconciliation. A changed request is a different operation, not a retry. Never delete the target guard or intent to force a second write.",
          "Issue-edit does not support write replay, including retry_after_authenticated_absence; reconcile exact retained target and preserve later human changes. Supported create/ready absence retry is explicit, authenticated and one-shot. Merge retries are always read-only; a still-open or uncertain PR remains blocked."
        ],
        "SEE ALSO": [
          "csdlc(1), csdlc-requests(1), csdlc-workflow(1), csdlc-finish(1)."
        ],
        "RETIRED SIMPLE FORMS": [
          "The create and close flag projections are retired writer syntax. They remain documented only to identify invocations that must migrate to github-issue ISSUE --operation OPERATION.json.",
          {
            "code": "csdlc github-issue create --repo OWNER/REPO --title TITLE \\\n  (--body BODY | --body-file PATH) --expected-head SHA \\\n  [--label LABEL]... [--assignee LOGIN]... [--milestone NUMBER] \\\n  [--credential-name NAME] [--execute]\ncsdlc github-issue close --repo OWNER/REPO --issue NUMBER \\\n  --disposition duplicate|superseded|no-op --rationale TEXT \\\n  (--body CURRENT_BODY | --body-file PATH) --expected-head SHA \\\n  [--duplicate-of NUMBER] [--credential-name NAME] [--execute]"
          },
          "Without --execute these forms print the same typed dispatch for inspection; --expected-head is still a full exact SHA and canonical selector discovery must succeed. --body/--body-file are mutually exclusive and one is required. --label/--assignee repeat; other duplicate flags fail. --milestone, --issue and --duplicate-of are positive numeric IDs. Duplicate close requires --duplicate-of. --disposition also accepts no_op. --credential-name selects an environment name (default GITHUB_TOKEN), never a token value.",
          "Close appends rationale and disposition to the authenticated current issue body, sets closed/not_planned, and retains the operation marker. A supplied current body containing an existing native operation marker is rejected by the current simple form; use a reviewed typed request preserving exact provenance rather than removing markers opportunistically.",
          "issue_edit optional metadata: omitted title/body/labels/assignees/milestone preserves the corresponding remote value. labels is {operation:add|remove|replace,names:[...]}; replacement [] clears. assignees [] clears. milestone is {operation:set,number:N} or {operation:clear}. Bare null metadata fails; explicit empty body clears prose while retaining native marker. Deltas are resolved once and intent/readback binds the resolved target."
        ]
      }
    },
    {
      "name": "csdlc-github-pr",
      "summary": "create, update, ready or merge a reviewed PR",
      "sections": {
        "SYNOPSIS": [
          {
            "code": "csdlc github-pr ISSUE --operation OPERATION.json [--execute]\ncsdlc github-pr --intent-request REQUEST.json\ncsdlc github-pr --help"
          }
        ],
        "ISSUE INTENT FORM": [
          "The semantic issue intent form is the current operational interface. It resolves issue identity and routes effects through the native semantic transaction owner.",
          "The intent form derives canonical target/review identities and previews unless --execute is present. PR creation follows the retained publication plan. Explicit title/body updates may differ while preserving current target/head/base, independent review and the canonical closing relation; they do not rewrite the plan used by ordinary publish. Minimal merge content is {action: pull_request_merge, base, method, operator_approval}; operator_approval is a real nonempty explicit authorization reference. Internal review receipt fields are rejected and derived from retained exact-head evidence. Native #849 linkage, review/check/base and reconciliation guards remain enforced.",
          "Optional --repo-root selects discovery, --json retains machine output, and --emit-request returns a generated wrapper accepted directly by COMMAND --intent-request REQUEST.json; its inner request is also accepted. Both forms share identity and mutation guards.",
          "The optional --preview explicitly selects the default observational intent mode and cannot be combined with --execute."
        ],
        "DESCRIPTION": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "With --execute owns pull_request_create, pull_request_update, pull_request_ready and pull_request_merge. Only this command owns merge. Merge requires explicit authorization naming the repository and numeric PR, matching independent exact-head review, merge method, base and authenticated policy/check admission. Unsupported policy and uncertain outcomes fail closed."
        ],
        "OPTIONS": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "--request: Required path to a UTF-8 JSON request of the command's request family.",
          "--observe-github: Perform authenticated GitHub readback. Requires selected credentials. It does not itself create, update or merge a PR.",
          "--execute: Select typed operational dispatch, not a generic force flag. Cannot combine with --observe-github on non-pr-state remote routes.",
          "--help: Show standalone help; -h is equivalent.",
          "-h: Show standalone help; do not combine with execution options.",
          "--intent-request: Execute an exact generated csdlc.v3.intent_request.v1 request or its generated wrapper; stale snapshots fail closed."
        ],
        "REQUEST": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "Without --execute: RemoteRouteRequest, including typed review/readback receipt references when required. With --execute on non-pr-state: OperationalRemoteDispatchRequest, whose expected_lifecycle_digest is the canonical authority selector payload digest, not the local issue index digest. See csdlc-requests(1).",
          "Non-pr-state --execute plus --observe-github is rejected. --observe-github without --execute uses authenticated readback in the planning path; it does not turn the returned plan into a performed mutation. Request family must match the selected mode."
        ],
        "EFFECTS": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "Readback/planning does not perform remote mutation. Operational github mutation writes durable intent before network dispatch and retains authenticated reconciliation/receipt. Successful review/publish plan reports are not PR creation. Credential names are scoped to the adapter; no raw token in argv or request."
        ],
        "EXAMPLES": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          {
            "code": "csdlc github-pr 870 --operation pr-operation.json --execute"
          },
          "Request fixtures are in docs/csdlc-v3/man/examples/; values are illustrative and must be bound to the actual head and reviewed issue. See csdlc-workflow(1) for the full live sequence."
        ],
        "RECOVERY": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "After uncertain dispatch retain the original request and exact intent. Reinvoke the identical native request for authenticated reconciliation. A changed request is a different operation, not a retry. Never delete the target guard or intent to force a second write.",
          "Issue-edit does not support write replay, including retry_after_authenticated_absence; reconcile exact retained target and preserve later human changes. Supported create/ready absence retry is explicit, authenticated and one-shot. Merge retries are always read-only; a still-open or uncertain PR remains blocked."
        ],
        "SEE ALSO": [
          "csdlc(1), csdlc-requests(1), csdlc-workflow(1), csdlc-finish(1)."
        ],
        "MERGE": [
          "mutation.action pull_request_merge requires base, method \"merge\", review_receipt_path and review_receipt_digest. Squash/rebase, queues, unsupported active rules, incomplete pagination, missing/wrong-app checks, skipped required checks, unresolved threads and changes-requested reviews fail closed.",
          "Admission observes exact PR/head/base and effective branch policy. The SHA conditional protects the head, not an atomic base/policy snapshot. Reconciliation requires the authenticated merge commit to have two ordered parents, matching retained base then reviewed head. A base race after dispatch can only be detected afterward; never claim atomic policy compare-and-swap or automatic rollback.",
          "After successful merge reconciliation run finish --observe-github independently. A merge receipt is not terminal issue-close evidence."
        ],
        "MERGE LINKAGE": [
          "Merge requires reviewed publication_linkage, one canonical whole-line Closes owner/repository#issue or Part of owner/repository#issue relation, complete authenticated closingIssuesReferences and a qualified OPEN issue before dispatch. Same-repository #issue is supported; split-repository references must be qualified. Closing poststate requires CLOSED, PartOf requires OPEN. Head CAS does not freeze body, base or policy; late drift blocks reconciliation and never authorizes another PUT. See docs/csdlc-v3/PULL_REQUEST_MERGE.md."
        ]
      }
    },
    {
      "name": "csdlc-pr-state",
      "summary": "observe PR state and publication linkage",
      "sections": {
        "SYNOPSIS": [
          {
            "code": "csdlc pr-state ISSUE"
          },
          {
            "code": "csdlc pr-state --request REQUEST [--observe-github] [--execute]\ncsdlc pr-state --help"
          }
        ],
        "ISSUE INTENT FORM": [
          "Semantic issue intent forms are the current operational contract. Direct issue-writer --request forms are retired and remain available only through --help and --describe for migration discovery. Observation and administrative request forms retain their documented contracts. See docs/csdlc-v3/INTENT_COMMANDS.md for strict content and effect boundaries.",
          "Observe canonical PR identity through authenticated native readback. This form accepts no mutation or preview override and does not create receipt authority from caller JSON.",
          "Optional --repo-root selects discovery, --json retains machine output, and --emit-request returns a generated wrapper accepted directly by COMMAND --intent-request REQUEST.json; its inner request is also accepted. Both forms share identity and mutation guards."
        ],
        "DESCRIPTION": [
          "The following details describe the retained request-based observation form. The current semantic issue form is specified above.",
          "Without --observe-github validates supplied retained receipts; with it obtains authenticated exact PR readback. No remote write is dispatched. The shared parser accepts --execute but pr-state ignores that selection and remains on its readback path; do not use it as a mutation switch. Current transport can create temporary local runtime metadata."
        ],
        "OPTIONS": [
          "The following details describe the retained request-based observation form. The current semantic issue form is specified above.",
          "--request: Required path to a UTF-8 JSON request of the command's request family.",
          "--observe-github: Perform authenticated GitHub readback. Requires selected credentials. It does not itself create, update or merge a PR.",
          "--execute: Select typed operational dispatch, not a generic force flag. Cannot combine with --observe-github on non-pr-state remote routes.",
          "--help: Show standalone help; -h is equivalent.",
          "-h: Show standalone help; do not combine with execution options."
        ],
        "REQUEST": [
          "The following details describe the retained request-based observation form. The current semantic issue form is specified above.",
          "Without --execute: RemoteRouteRequest, including typed review/readback receipt references when required. With --execute on non-pr-state: OperationalRemoteDispatchRequest, whose expected_lifecycle_digest is the canonical authority selector payload digest, not the local issue index digest. See csdlc-requests(1).",
          "Non-pr-state --execute plus --observe-github is rejected. --observe-github without --execute uses authenticated readback in the planning path; it does not turn the returned plan into a performed mutation. Request family must match the selected mode."
        ],
        "EFFECTS": [
          "The following details describe the retained request-based observation form. The current semantic issue form is specified above.",
          "Readback/planning does not perform remote mutation. Operational github mutation writes durable intent before network dispatch and retains authenticated reconciliation/receipt. Successful review/publish plan reports are not PR creation. Credential names are scoped to the adapter; no raw token in argv or request."
        ],
        "EXAMPLES": [
          "The following details describe the retained request-based observation form. The current semantic issue form is specified above.",
          {
            "code": "csdlc pr-state --request remote-request.json --observe-github"
          },
          "Request fixtures are in docs/csdlc-v3/man/examples/; values are illustrative and must be bound to the actual head and reviewed issue. See csdlc-workflow(1) for the full live sequence."
        ],
        "RECOVERY": [
          "The following details describe the retained request-based observation form. The current semantic issue form is specified above.",
          "After uncertain dispatch retain the original request and exact intent. Reinvoke the identical native request for authenticated reconciliation. A changed request is a different operation, not a retry. Never delete the target guard or intent to force a second write.",
          "Issue-edit does not support write replay, including retry_after_authenticated_absence; reconcile exact retained target and preserve later human changes. Supported create/ready absence retry is explicit, authenticated and one-shot. Merge retries are always read-only; a still-open or uncertain PR remains blocked."
        ],
        "SEE ALSO": [
          "csdlc(1), csdlc-requests(1), csdlc-workflow(1), csdlc-finish(1)."
        ]
      }
    },
    {
      "name": "csdlc-publish",
      "summary": "check reviewed publication admission",
      "sections": {
        "SYNOPSIS": [
          {
            "code": "csdlc publish ISSUE\ncsdlc publish --intent-request REQUEST.json\ncsdlc publish --help"
          }
        ],
        "ISSUE INTENT FORM": [
          "The semantic issue intent form is the current operational interface. It resolves issue identity and routes effects through the native semantic transaction owner.",
          "The intent form requires retained independent exact-head review and proof, resolves canonical PR identity from native mutation intents/receipts, creates or updates through the GitHub owner, then performs authenticated publication readback. A failed post-mutation readback preserves performed/unknown effects. --preview plan admits without mutation. No hidden Git push is added.",
          "Optional --repo-root selects discovery, --json retains machine output, and --emit-request returns a generated wrapper accepted directly by COMMAND --intent-request REQUEST.json; its inner request is also accepted. Both forms share identity and mutation guards."
        ],
        "DESCRIPTION": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "Validates typed independent review, exact head, closing/part-of linkage and readback evidence. Without --execute produces a non-authoritative plan. With --execute requires an operational dispatch with operation.kind publish and canonical authority; it still validates a publication plan and does not create the GitHub PR. Use github-pr for the actual publication transport after successful review/admission."
        ],
        "OPTIONS": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "--request: Required path to a UTF-8 JSON request of the command's request family.",
          "--observe-github: Perform authenticated GitHub readback. Requires selected credentials. It does not itself create, update or merge a PR.",
          "--execute: Select typed operational dispatch, not a generic force flag. Cannot combine with --observe-github on non-pr-state remote routes.",
          "--help: Show standalone help; -h is equivalent.",
          "-h: Show standalone help; do not combine with execution options.",
          "--intent-request: Execute an exact generated csdlc.v3.intent_request.v1 request or its generated wrapper; stale snapshots fail closed."
        ],
        "REQUEST": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "Without --execute: RemoteRouteRequest, including typed review/readback receipt references when required. With --execute on non-pr-state: OperationalRemoteDispatchRequest, whose expected_lifecycle_digest is the canonical authority selector payload digest, not the local issue index digest. See csdlc-requests(1).",
          "Non-pr-state --execute plus --observe-github is rejected. --observe-github without --execute uses authenticated readback in the planning path; it does not turn the returned plan into a performed mutation. Request family must match the selected mode."
        ],
        "EFFECTS": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "Readback/planning does not perform remote mutation. Operational github mutation writes durable intent before network dispatch and retains authenticated reconciliation/receipt. Successful review/publish plan reports are not PR creation. Credential names are scoped to the adapter; no raw token in argv or request."
        ],
        "EXAMPLES": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          {
            "code": "csdlc publish 870"
          },
          "Request fixtures are in docs/csdlc-v3/man/examples/; values are illustrative and must be bound to the actual head and reviewed issue. See csdlc-workflow(1) for the full live sequence."
        ],
        "RECOVERY": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "After uncertain dispatch retain the original request and exact intent. Reinvoke the identical native request for authenticated reconciliation. A changed request is a different operation, not a retry. Never delete the target guard or intent to force a second write.",
          "Issue-edit does not support write replay, including retry_after_authenticated_absence; reconcile exact retained target and preserve later human changes. Supported create/ready absence retry is explicit, authenticated and one-shot. Merge retries are always read-only; a still-open or uncertain PR remains blocked."
        ],
        "SEE ALSO": [
          "csdlc(1), csdlc-requests(1), csdlc-workflow(1), csdlc-finish(1)."
        ]
      }
    },
    {
      "name": "csdlc-review",
      "summary": "validate independent exact-head review evidence",
      "sections": {
        "SYNOPSIS": [
          {
            "code": "csdlc review ISSUE --evidence REVIEW.json\ncsdlc review --intent-request REQUEST.json\ncsdlc review --help"
          }
        ],
        "ISSUE INTENT FORM": [
          "The semantic issue intent form is the current operational interface. It resolves issue identity and routes effects through the native semantic transaction owner.",
          "Supply csdlc.v3.review_judgment.v1 with implementer, independent reviewer, reviewed_revision, verdict pass and actual evidence text. The owner derives proof and receipt bookkeeping and retains one combined review record. Legacy receipt/receipt_digest/proof_path/proof_digest inputs remain readable. Same-principal review, stale revisions, empty evidence and non-passing verdicts are rejected; the tool does not perform the independent review. --preview plan is read-only.",
          "Optional --repo-root selects discovery, --json retains machine output, and --emit-request returns a generated wrapper accepted directly by COMMAND --intent-request REQUEST.json; its inner request is also accepted. Both forms share identity and mutation guards."
        ],
        "DESCRIPTION": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "Checks that implementer/reviewer are not the same nonempty principal and that a review revision is supplied. The current review classifier does not authenticate the typed receipt contents or perform the human/subagent review; loading a receipt is not evidence that this route validated it. Independent review and a matching typed receipt remain required by policy and later publication checks. Without --execute produces a non-authoritative plan; with it requires operational envelope operation.kind review and authenticated authority."
        ],
        "OPTIONS": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "--request: Required path to a UTF-8 JSON request of the command's request family.",
          "--observe-github: Perform authenticated GitHub readback. Requires selected credentials. It does not itself create, update or merge a PR.",
          "--execute: Select typed operational dispatch, not a generic force flag. Cannot combine with --observe-github on non-pr-state remote routes.",
          "--help: Show standalone help; -h is equivalent.",
          "-h: Show standalone help; do not combine with execution options.",
          "--intent-request: Execute an exact generated csdlc.v3.intent_request.v1 request or its generated wrapper; stale snapshots fail closed."
        ],
        "REQUEST": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "Without --execute: RemoteRouteRequest, including typed review/readback receipt references when required. With --execute on non-pr-state: OperationalRemoteDispatchRequest, whose expected_lifecycle_digest is the canonical authority selector payload digest, not the local issue index digest. See csdlc-requests(1).",
          "Non-pr-state --execute plus --observe-github is rejected. --observe-github without --execute uses authenticated readback in the planning path; it does not turn the returned plan into a performed mutation. Request family must match the selected mode."
        ],
        "EFFECTS": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "Readback/planning does not perform remote mutation. Operational github mutation writes durable intent before network dispatch and retains authenticated reconciliation/receipt. Successful review/publish plan reports are not PR creation. Credential names are scoped to the adapter; no raw token in argv or request."
        ],
        "EXAMPLES": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          {
            "code": "csdlc review 870 --evidence review-evidence.json"
          },
          "Request fixtures are in docs/csdlc-v3/man/examples/; values are illustrative and must be bound to the actual head and reviewed issue. See csdlc-workflow(1) for the full live sequence."
        ],
        "RECOVERY": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "After uncertain dispatch retain the original request and exact intent. Reinvoke the identical native request for authenticated reconciliation. A changed request is a different operation, not a retry. Never delete the target guard or intent to force a second write.",
          "Issue-edit does not support write replay, including retry_after_authenticated_absence; reconcile exact retained target and preserve later human changes. Supported create/ready absence retry is explicit, authenticated and one-shot. Merge retries are always read-only; a still-open or uncertain PR remains blocked."
        ],
        "SEE ALSO": [
          "csdlc(1), csdlc-requests(1), csdlc-workflow(1), csdlc-finish(1)."
        ]
      }
    },
    {
      "name": "csdlc-finish",
      "summary": "reconcile terminal issue truth",
      "sections": {
        "SYNOPSIS": [
          {
            "code": "csdlc finish ISSUE [--pull-request NUMBER [--publication-repository OWNER/NAME] | --disposition DISPOSITION.json]\ncsdlc finish --intent-request REQUEST.json\ncsdlc finish --help"
          }
        ],
        "ISSUE INTENT FORM": [
          "The semantic issue intent form is the current operational interface. It resolves issue identity and routes effects through the native semantic transaction owner.",
          "The intent form derives canonical PR identity and authenticates merged PR plus closed issue through the existing terminal owner. Successful execution retains terminal state and receipt in primary Git metadata. --preview plan observes without persistence. It does not merge or close the issue itself.",
          "Optional --repo-root selects discovery, --json retains machine output, and --emit-request returns a generated wrapper accepted directly by COMMAND --intent-request REQUEST.json; its inner request is also accepted. Both forms share identity and mutation guards.",
          "Optional --disposition supplies strict disposition/operator/rationale/evidence_refs for native no-PR terminal reconciliation. Disposition uses retired_without_execution, duplicate, superseded, absorbed, coordination_completed or historical_disposition. The adapter authenticates closed issue timestamps internally and the native owner rechecks them before persistence; no caller timestamps or internal receipts are required. Administrative disposition is not merged implementation proof.",
          "For a PR created outside the native publication owner, supply --pull-request NUMBER. If that PR belongs to another repository, also supply --publication-repository OWNER/NAME. These are only explicit target hints: the existing terminal owner authenticates the exact issue/head, merged PR, qualified closing relation and closed issue before persisting terminal truth. Current semantic issues still complete the ordinary Finish transition; only genuine legacy migration state uses compatibility reconciliation. Conflicting native or durable terminal identities are rejected. No publication mutation receipt is fabricated. These flags cannot be combined with --disposition; generated intent content retains both identities. After successful reconciliation, ordinary finish can use the validated retained terminal receipt for idempotent readback."
        ],
        "DESCRIPTION": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "Requires authenticated GitHub observation of a merged PR with the exact expected head, closing linkage and closed issue. mode must be closing; part_of checkpoints cannot finish. --observe-github is necessary for public authenticated finish; caller-authored public_adapter_receipt is not trusted. terminal_state is optional: omit to observe, include canonical output paths to retain terminal state and receipt. Finish does not merge or close GitHub issues."
        ],
        "OPTIONS": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "--request: Required path to a UTF-8 JSON request of the command's request family.",
          "--observe-github: Perform authenticated GitHub readback. Requires selected credentials. It does not itself create, update or merge a PR.",
          "--publication-repository: Qualified owner/name repository containing the explicit closing PR; valid only with --pull-request. The target issue remains in the issue repository.",
          "--help: Show standalone help; -h is equivalent.",
          "-h: Show standalone help; do not combine with execution options.",
          "--intent-request: Execute an exact generated csdlc.v3.intent_request.v1 request or its generated wrapper; stale snapshots fail closed."
        ],
        "REQUEST": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "TerminalRouteRequest, with only the relevant finish, cleanup or cutover fields. See csdlc-requests(1). Examples under docs/csdlc-v3/man/examples use fixture identities and are not permission to mutate those targets."
        ],
        "EFFECTS": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "Authenticated readback plus optional local terminal persistence; no GitHub write. A successful terminal observation without terminal_state does not create a durable terminal receipt."
        ],
        "FILES": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "Primary terminal paths: resolved Git metadata csdlc-v3/local/v3/issues/ISSUE/terminal.json and csdlc-v3/local/evidence/ISSUE/terminal-receipt.json. Linked paths: .csdlc/v3/issues/ISSUE/terminal.json and .csdlc/evidence/ISSUE/terminal-receipt.json. Request terminal_state.repository_root must be the real reconciliation checkout; state_path and receipt_path must resolve exactly to those locations."
        ],
        "EXAMPLES": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          {
            "code": "csdlc finish 870"
          },
          "Use csdlc-workflow(1) for ordered finish and cleanup; cutover/rollback examples validate the schema and denial boundary, not a new transition authorization."
        ],
        "RECOVERY": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "Retain original evidence and exact current checkout identity. Stale terminal state requires a matching expected_state_digest; mismatched receipt identity must be investigated, never overwritten. Refresh a clean preview after any target/head/registration change. Unsupported or unproven transition states fail closed; do not fall back to deleted wrappers."
        ],
        "SEE ALSO": [
          "csdlc(1), csdlc-requests(1), csdlc-workflow(1)."
        ]
      }
    },
    {
      "name": "csdlc-clean",
      "summary": "preview and remove an eligible registered worktree",
      "sections": {
        "SYNOPSIS": [
          {
            "code": "csdlc clean ISSUE\ncsdlc clean ISSUE --execute --preview TOKEN\ncsdlc clean --intent-request REQUEST.json\ncsdlc clean --help"
          }
        ],
        "ISSUE INTENT FORM": [
          "The semantic issue intent form is the current operational interface. It resolves issue identity and routes effects through the native semantic transaction owner.",
          "The intent preview returns preview_token on stdout without a token file. Execution compares the issue generation/digest, authority, exact target/head, terminal receipt, topology, policy and native preview before the owner repeats cleanup admission. Dirty/live/stale targets remain refused. Retained terminal evidence plus no registered issue binding can establish an observational repeated-clean no-op, never permission to remove another checkout.",
          "Optional --repo-root selects discovery, --json retains machine output, and --emit-request returns a generated wrapper accepted directly by COMMAND --intent-request REQUEST.json; its inner request is also accepted. Both forms share identity and mutation guards.",
          "The candidate intent cleanup bridge archives only exact generated untracked issue cards/evidence/completed transaction records and lock after admission, verifies durable copied bytes/permissions/symlink text, then removes originals with the index last. Tracked changes and unrelated untracked bytes remain refused. Retained request-based cleanup remains unchanged. After interrupted removal, obtain a fresh clean preview and explicitly continue cleanup. Verified archived index evidence can authorize only cleanup continuation; it does not restore implementation authority."
        ],
        "DESCRIPTION": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "Requires durable terminal receipt identity and digest, actual registered candidate, approved parent, exact matching head and clean non-live checkout. First use cleanup.remove false; inspect Removable and save returned receipt_digest. Then use remove true with that preview_receipt_digest and unchanged target identity. Removal is separate from finish. Dirty/live/unregistered/protected paths are not permission to delete manually."
        ],
        "OPTIONS": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "--request: Required path to a UTF-8 JSON request of the command's request family.",
          "--help: Show standalone help; -h is equivalent.",
          "-h: Show standalone help; do not combine with execution options.",
          "--intent-request: Execute an exact generated csdlc.v3.intent_request.v1 request or its generated wrapper; stale snapshots fail closed."
        ],
        "REQUEST": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "TerminalRouteRequest, with only the relevant finish, cleanup or cutover fields. See csdlc-requests(1). Examples under docs/csdlc-v3/man/examples use fixture identities and are not permission to mutate those targets."
        ],
        "EFFECTS": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "Preview inspects; remove true may remove exactly the registered candidate after repeated guards. It does not authorize broad branch/worktree cleanup."
        ],
        "FILES": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "Primary terminal paths: resolved Git metadata csdlc-v3/local/v3/issues/ISSUE/terminal.json and csdlc-v3/local/evidence/ISSUE/terminal-receipt.json. Linked paths: .csdlc/v3/issues/ISSUE/terminal.json and .csdlc/evidence/ISSUE/terminal-receipt.json. Request terminal_state.repository_root must be the real reconciliation checkout; state_path and receipt_path must resolve exactly to those locations."
        ],
        "EXAMPLES": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          {
            "code": "csdlc clean 870\ncsdlc clean 870 --execute --preview TOKEN"
          },
          "Use csdlc-workflow(1) for ordered finish and cleanup; cutover/rollback examples validate the schema and denial boundary, not a new transition authorization."
        ],
        "RECOVERY": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "Retain original evidence and exact current checkout identity. Stale terminal state requires a matching expected_state_digest; mismatched receipt identity must be investigated, never overwritten. Refresh a clean preview after any target/head/registration change. Unsupported or unproven transition states fail closed; do not fall back to deleted wrappers."
        ],
        "SEE ALSO": [
          "csdlc(1), csdlc-requests(1), csdlc-workflow(1)."
        ]
      }
    },
    {
      "name": "csdlc-cutover",
      "summary": "apply an explicitly authorized authority transition",
      "sections": {
        "SYNOPSIS": [
          {
            "code": "csdlc cutover 505 --operation OPERATION.json [--execute]\ncsdlc cutover --intent-request REQUEST.json\ncsdlc cutover --help"
          }
        ],
        "ISSUE INTENT FORM": [
          "The semantic issue intent form is the current operational interface. It resolves the authenticated repository and issue context, reserves the administrative semantic operation before any effect and retains the exact native outcome.",
          "OPERATION.json is a complete TerminalRouteRequest whose cutover.operation is apply. Its repository, issue 505, primary repository root and nested cutover.execute value must exactly match the semantic invocation. --execute is required for mutation; omission produces a read-only preflight.",
          "Optional --repo-root selects discovery, --json retains machine output, and --emit-request returns a generated wrapper accepted directly by COMMAND --intent-request REQUEST.json; its inner request is also accepted. Both forms share identity, approval and mutation guards."
        ],
        "DESCRIPTION": [
          "The legacy --request writer syntax and --observe-github flag are retired. They remain visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current administrative interface.",
          "Retained authority-transition owner for an explicitly operator-reviewed cutover. It requires cutover approval, source provenance, rollback evidence, undo boundary and authenticated #505 evidence. Historical approval cannot authorize a new transition."
        ],
        "OPTIONS": [
          "--operation: Required path to explicit operation content. The owner derives repository and issue identity from the semantic context.",
          "--execute: Request the guarded administrative effect. Omit it for read-only preview.",
          "--intent-request: Execute an exact generated csdlc.v3.intent_request.v1 request or its generated wrapper; stale snapshots fail closed.",
          "--repo-root: Select repository discovery; it cannot override bound issue identity.",
          "--json: Retain machine-readable JSON output.",
          "--emit-request: Emit the generated semantic request without executing it.",
          "--request: Retired direct-writer input retained only for migration discovery.",
          "--observe-github: Retired direct-writer flag retained only for migration discovery.",
          "--help: Show standalone help; -h is equivalent.",
          "-h: Show standalone help; do not combine with execution options."
        ],
        "OPERATION": [
          "TerminalRouteRequest with cutover.operation apply. The semantic adapter requires issue 505 and exact repository, repository-root and execute identity before the terminal owner is called. See csdlc-requests(1)."
        ],
        "EFFECTS": [
          "Preview is read-only. An admitted --execute invocation may replace the selected binary or authority selector and retain the guarded transition receipt; proof and preview do not authorize execution."
        ],
        "FILES": [
          "Primary terminal paths use resolved Git metadata under csdlc-v3/local/v3/issues/505 and csdlc-v3/local/evidence/505. Linked paths remain under .csdlc. The operation repository root must be the exact primary reconciliation checkout."
        ],
        "EXAMPLES": [
          "The legacy --request writer syntax and --observe-github flag are retired. They remain visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current administrative interface.",
          {
            "code": "csdlc cutover 505 --operation cutover-operation.json\ncsdlc cutover 505 --operation cutover-operation.json --execute"
          },
          "Examples validate syntax and the denial boundary; they do not grant transition authorization."
        ],
        "RECOVERY": [
          "Retain the original operation and exact checkout identity. A pending administrative transaction is reconciled from its retained canonical request; caller replacement bytes are not accepted. Unsupported or unproven state fails closed."
        ],
        "SEE ALSO": [
          "csdlc(1), csdlc-requests(1), csdlc-workflow(1)."
        ]
      }
    },
    {
      "name": "csdlc-rollback",
      "summary": "invoke the typed rollback transition",
      "sections": {
        "SYNOPSIS": [
          {
            "code": "csdlc rollback 505 --operation OPERATION.json [--execute]\ncsdlc rollback --intent-request REQUEST.json\ncsdlc rollback --help"
          }
        ],
        "ISSUE INTENT FORM": [
          "The semantic issue intent form is the current operational interface. It resolves the authenticated repository and issue context, reserves the administrative semantic operation before any effect and retains the exact native outcome.",
          "OPERATION.json is a complete TerminalRouteRequest whose cutover.operation is rollback. Its repository, issue 505, primary repository root and nested cutover.execute value must exactly match the semantic invocation. --execute is required for mutation; omission produces a read-only preflight.",
          "Optional --repo-root selects discovery, --json retains machine output, and --emit-request returns a generated wrapper accepted directly by COMMAND --intent-request REQUEST.json; its inner request is also accepted. Both forms share identity, approval and mutation guards."
        ],
        "DESCRIPTION": [
          "The legacy --request writer syntax and --observe-github flag are retired. They remain visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current administrative interface.",
          "Typed rollback authority-transition owner. Exact explicit operator authorization and retained rollback proof remain mandatory; unavailable v3 authority does not automatically enable v2."
        ],
        "OPTIONS": [
          "--operation: Required path to explicit operation content. The owner derives repository and issue identity from the semantic context.",
          "--execute: Request the guarded administrative effect. Omit it for read-only preview.",
          "--intent-request: Execute an exact generated csdlc.v3.intent_request.v1 request or its generated wrapper; stale snapshots fail closed.",
          "--repo-root: Select repository discovery; it cannot override bound issue identity.",
          "--json: Retain machine-readable JSON output.",
          "--emit-request: Emit the generated semantic request without executing it.",
          "--request: Retired direct-writer input retained only for migration discovery.",
          "--observe-github: Retired direct-writer flag retained only for migration discovery.",
          "--help: Show standalone help; -h is equivalent.",
          "-h: Show standalone help; do not combine with execution options."
        ],
        "OPERATION": [
          "TerminalRouteRequest with cutover.operation rollback. The semantic adapter requires issue 505 and exact repository, repository-root and execute identity before the terminal owner is called. See csdlc-requests(1)."
        ],
        "EFFECTS": [
          "Preview is read-only. An admitted --execute invocation may replace the selected binary or authority selector and retain the guarded transition receipt; proof and preview do not authorize execution."
        ],
        "FILES": [
          "Primary terminal paths use resolved Git metadata under csdlc-v3/local/v3/issues/505 and csdlc-v3/local/evidence/505. Linked paths remain under .csdlc. The operation repository root must be the exact primary reconciliation checkout."
        ],
        "EXAMPLES": [
          "The legacy --request writer syntax and --observe-github flag are retired. They remain visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current administrative interface.",
          {
            "code": "csdlc rollback 505 --operation rollback-operation.json\ncsdlc rollback 505 --operation rollback-operation.json --execute"
          },
          "Examples validate syntax and the denial boundary; they do not grant transition authorization."
        ],
        "RECOVERY": [
          "Retain the original operation and exact checkout identity. A pending administrative transaction is reconciled from its retained canonical request; caller replacement bytes are not accepted. Unsupported or unproven state fails closed."
        ],
        "SEE ALSO": [
          "csdlc(1), csdlc-requests(1), csdlc-workflow(1)."
        ]
      }
    },
    {
      "name": "csdlc-proof",
      "summary": "execute and retain an issue-bound proof manifest",
      "sections": {
        "SYNOPSIS": [
          {
            "code": "csdlc proof ISSUE\ncsdlc proof --intent-request REQUEST.json\ncsdlc proof --help"
          }
        ],
        "ISSUE INTENT FORM": [
          "The semantic issue intent form is the current operational interface. It resolves issue identity and routes effects through the native semantic transaction owner.",
          "Execute the retained plan validators through the native proof owner. Require admitted Cargo test arguments, current candidate bytes, successful nonzero test counts and fresh issue/worktree identity; retain actual outcomes in .csdlc/evidence/ISSUE/intent-proof.json. This form rejects preview/execute overrides.",
          "Optional --repo-root selects discovery, --json retains machine output, and --emit-request returns a generated wrapper accepted directly by COMMAND --intent-request REQUEST.json; its inner request is also accepted. Both forms share identity and mutation guards.",
          "Validators admit optional timeout_seconds from 1 through 300, default 300. The Unix owner creates an owned process group, captures stdout/stderr without blocking reader threads and bounds timeout/cancellation reap/drain to two additional seconds. Scoped serialized SIGINT/SIGTERM handling is restored after execution; receipts distinguish timed_out, cancelled and cleanup_complete and include actual elapsed time. Non-Unix execution is explicitly unsupported. Proof refuses ignored untracked source/configuration outside native .csdlc artifacts and explicit disposable Rust cache roots. Those exemptions do not admit executable inputs: manifests, target sources and repository path dependencies must be tracked, and compiler dependency records reject excluded repository module/include inputs. Unsupported generated repository source is refused. Exact captured-stream digests and bounded non-lossless diagnostic excerpts are retained; sensitive-key lines are omitted and repository/HOME paths replaced. Input drift or persistence refusal retains failed execution output without bypassing native evidence guards. The digest binds repository tracked bytes and declared validators; ambient HOME Cargo configuration, external toolchains and cached dependencies are not fully fingerprinted, so this is not hermetic build evidence.",
          "Each validator receives a newly created private mode-0700 temporary directory directly beneath its canonical bound worktree, named .csdlc-validator-tmp-PID-SEQUENCE. TMPDIR, TMP and TEMP all select that directory; parent temporary-directory variables are ignored. The owner rejects noncanonical roots, roots owned by another OS user, group/world-writable roots, and existing temporary paths (including symlinks). It never adopts an existing directory. Validators inherit only the explicitly reconstructed environment; this does not enable arbitrary environment passthrough.",
          "The native Cargo target is WORKTREE/target/intent-validation. Warm trusted same-host dependency artifacts into that exact target before proof; do not change HOME or global Cargo configuration. Each validator outcome records temporary_root and cargo_target relative to its bound worktree. Temporary contents are removed after process cleanup, including failure and cancellation; cleanup_complete remains false if directory cleanup fails. Stream digests and proof outcomes remain durable. This controls standard temporary-directory APIs, not arbitrary absolute paths written explicitly by validator code."
        ],
        "DESCRIPTION": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "Runs the declared deterministic V3 command through bounded argv/request/evidence paths, normalizes its result and retains proof evidence. Requires authenticated invoking linked worktree ownership and exact generation/digest/head/common-Git binding. Provider side effects, stale evidence and unsupported normalization fail closed. This is focused command proof, not broad semantic equivalence."
        ],
        "OPTIONS": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "--request: Required path to a UTF-8 JSON request of the command's request family.",
          "--help: Show standalone help; -h is equivalent.",
          "-h: Show standalone help; do not combine with execution options.",
          "--intent-request: Execute an exact generated csdlc.v3.intent_request.v1 request or its generated wrapper; stale snapshots fail closed."
        ],
        "REQUEST": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "ProofRouteRequest and its typed binding/manifest/install fields are described in csdlc-requests(1). Paths must stay within the authenticated bound issue evidence boundary; they are not arbitrary shell execution or filesystem destinations."
        ],
        "EFFECTS": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          "Executes declared subprocess and writes canonical proof artifacts on admitted execution; blocked proof is not successful validation."
        ],
        "EXAMPLES": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current operational interface.",
          {
            "code": "csdlc proof 870"
          },
          "Fixture examples are parse-checked; historical shadow/soak examples intentionally exercise their blocked behavior. Proof/install require actual current binding and valid referenced artifacts for success."
        ],
        "DIAGNOSTICS": [
          "Blocked reports use schema csdlc.v3.proof_route.v1 on stdout and a short stderr message with exit 2. Missing/stale binding, path escape, artifact digest mismatch and unauthorized execution remain blockers. Inspect status and findings before calling a proof complete."
        ],
        "SEE ALSO": [
          "csdlc(1), csdlc-requests(1), csdlc-workflow(1)."
        ]
      }
    },
    {
      "name": "csdlc-install",
      "summary": "validate or execute an issue-bound binary installation",
      "sections": {
        "SYNOPSIS": [
          {
            "code": "csdlc install ISSUE --operation OPERATION.json [--execute]\ncsdlc install --intent-request REQUEST.json\ncsdlc install --help"
          }
        ],
        "ISSUE INTENT FORM": [
          "The semantic issue intent form is the current operational interface. It derives current binding and authority inputs, reserves RecordInstall before the effect and retains the exact native witness.",
          "OPERATION.json is an InstallPlanInput. The owner builds the native ProofRouteRequest from authenticated issue context. --execute controls mutation; the operation content alone does not authorize installation.",
          "Optional --repo-root selects discovery, --json retains machine output, and --emit-request returns a generated wrapper accepted directly by COMMAND --intent-request REQUEST.json; its inner request is also accepted. Both forms share provenance, identity and mutation guards."
        ],
        "DESCRIPTION": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current administrative interface.",
          "Validates source provenance, selected binary and selector metadata digests plus the stable destination. Execution requires current bound issue ownership and matching explicit exact-head cutover approval proof. The destination is exactly .adl/bin/csdlc in the bound checkout. This is separate from adl/tools/install_owner_binaries.sh, which installs operational owners and man pages to .adl/bin/native-v3."
        ],
        "OPTIONS": [
          "--operation: Required path to explicit operation content. The owner derives repository and issue identity from the semantic context.",
          "--execute: Request the guarded administrative effect. Omit it for read-only preview.",
          "--intent-request: Execute an exact generated csdlc.v3.intent_request.v1 request or its generated wrapper; stale snapshots fail closed.",
          "--repo-root: Select repository discovery; it cannot override bound issue identity.",
          "--json: Retain machine-readable JSON output.",
          "--emit-request: Emit the generated semantic request without executing it.",
          "--request: Retired direct-writer input retained only for migration discovery.",
          "--help: Show standalone help; -h is equivalent.",
          "-h: Show standalone help; do not combine with execution options."
        ],
        "OPERATION": [
          "InstallPlanInput containing the selected binary, selector metadata, digests and approval evidence required by the native installer. See csdlc-requests(1) for the underlying proof/install fields."
        ],
        "EFFECTS": [
          "Preview validates the admitted preimage without deployment. An admitted --execute invocation copies the verified binary, sets executable permissions and retains the exact install receipt."
        ],
        "EXAMPLES": [
          "The legacy --request writer syntax is retired. It remains visible only through --help and --describe for migration discovery; execution fails before request parsing or effects. The semantic issue form above is the current administrative interface.",
          {
            "code": "csdlc install 870 --operation install-operation.json\ncsdlc install 870 --operation install-operation.json --execute"
          },
          "Fixture operation content does not establish current binding, provenance or approval proof."
        ],
        "RECOVERY": [
          "A pending installation is inspected and reconciled from the exact retained request. Resume requires the current recovery preview digest; partial or foreign destinations remain recovery-required."
        ],
        "DIAGNOSTICS": [
          "Blocked output identifies stale binding, provenance, destination, approval or retained transaction evidence. Preview success is not installation proof."
        ],
        "SEE ALSO": [
          "csdlc(1), csdlc-proof(1), csdlc-requests(1), csdlc-workflow(1)."
        ]
      }
    },
    {
      "name": "csdlc-shadow",
      "summary": "report the retired shadow execution boundary",
      "sections": {
        "SYNOPSIS": [
          {
            "code": "csdlc shadow --request REQUEST\ncsdlc shadow --help"
          }
        ],
        "DESCRIPTION": [
          "Historical compatibility route. Execution is disabled and reports blocked; no current v2/v3 equivalence proof or operational authorization is granted. Retained ShadowComparison inputs remain parseable for historical evidence only."
        ],
        "OPTIONS": [
          "--request: Required path to a UTF-8 JSON request of the command's request family.",
          "--help: Show standalone help; -h is equivalent.",
          "-h: Show standalone help; do not combine with execution options."
        ],
        "REQUEST": [
          "ProofRouteRequest and its typed binding/manifest/install fields are described in csdlc-requests(1). Paths must stay within the authenticated bound issue evidence boundary; they are not arbitrary shell execution or filesystem destinations."
        ],
        "EFFECTS": [
          "Read-only blocked historical classification; no execution."
        ],
        "EXAMPLES": [
          {
            "code": "csdlc shadow --request shadow-request.json"
          },
          "Fixture examples are parse-checked; historical shadow/soak examples intentionally exercise their blocked behavior. Proof/install require actual current binding and valid referenced artifacts for success."
        ],
        "DIAGNOSTICS": [
          "Blocked reports use schema csdlc.v3.proof_route.v1 on stdout and a short stderr message with exit 2. Missing/stale binding, path escape, artifact digest mismatch and unauthorized execution remain blockers. Inspect status and findings before calling a proof complete."
        ],
        "SEE ALSO": [
          "csdlc(1), csdlc-requests(1), csdlc-workflow(1)."
        ]
      }
    },
    {
      "name": "csdlc-soak",
      "summary": "report the retired soak execution boundary",
      "sections": {
        "SYNOPSIS": [
          {
            "code": "csdlc soak --request REQUEST\ncsdlc soak --help"
          }
        ],
        "DESCRIPTION": [
          "Historical compatibility route. Execution is disabled and reports blocked; no current soak/service/heartbeat proof is granted. Retained SoakEvidence inputs remain parseable for historical evidence only."
        ],
        "OPTIONS": [
          "--request: Required path to a UTF-8 JSON request of the command's request family.",
          "--help: Show standalone help; -h is equivalent.",
          "-h: Show standalone help; do not combine with execution options."
        ],
        "REQUEST": [
          "ProofRouteRequest and its typed binding/manifest/install fields are described in csdlc-requests(1). Paths must stay within the authenticated bound issue evidence boundary; they are not arbitrary shell execution or filesystem destinations."
        ],
        "EFFECTS": [
          "Read-only blocked historical classification; no execution."
        ],
        "EXAMPLES": [
          {
            "code": "csdlc soak --request soak-request.json"
          },
          "Fixture examples are parse-checked; historical shadow/soak examples intentionally exercise their blocked behavior. Proof/install require actual current binding and valid referenced artifacts for success."
        ],
        "DIAGNOSTICS": [
          "Blocked reports use schema csdlc.v3.proof_route.v1 on stdout and a short stderr message with exit 2. Missing/stale binding, path escape, artifact digest mismatch and unauthorized execution remain blockers. Inspect status and findings before calling a proof complete."
        ],
        "SEE ALSO": [
          "csdlc(1), csdlc-requests(1), csdlc-workflow(1)."
        ]
      }
    },
    {
      "name": "csdlc-foundation",
      "summary": "inspect retained foundation contract",
      "sections": {
        "SYNOPSIS": [
          {
            "code": "csdlc foundation --repo-root ROOT"
          }
        ],
        "DESCRIPTION": [
          "Reads docs/csdlc-v3 foundation contract, predecessor coverage and proportional lifecycle inputs; produces static construction classification. It does not grant current authority or create lifecycle state."
        ],
        "OPTIONS": [
          "--repo-root: Explicit repository discovery root. Required by foundation/sprint; optional for local-family commands. It does not override authenticated binding guards.",
          "--help: Show standalone help; -h is equivalent.",
          "-h: Show standalone help; do not combine with execution options."
        ],
        "REQUEST": [
          "No JSON request."
        ],
        "EFFECTS": [
          "Observation or static model only. Read findings/status, not just process exit status; no release or lifecycle activation authority is granted."
        ],
        "EXAMPLES": [
          {
            "code": "csdlc foundation --repo-root ."
          }
        ],
        "SEE ALSO": [
          "csdlc(1), csdlc-requests(1)."
        ]
      }
    },
    {
      "name": "csdlc-remote",
      "summary": "list remote command routes",
      "sections": {
        "SYNOPSIS": [
          {
            "code": "csdlc remote --help"
          }
        ],
        "DESCRIPTION": [
          "Read-only overview of github, github-issue, github-pr, pr-state, publish and review. No request is accepted; invocation with no arguments also prints help. See each command page for its actual mutation boundary."
        ],
        "OPTIONS": [
          "--help: Show standalone help; -h is equivalent.",
          "-h: Show standalone help; do not combine with execution options."
        ],
        "REQUEST": [
          "No JSON request."
        ],
        "EFFECTS": [
          "Observation or static model only. Read findings/status, not just process exit status; no release or lifecycle activation authority is granted."
        ],
        "EXAMPLES": [
          {
            "code": "csdlc remote --help"
          }
        ],
        "SEE ALSO": [
          "csdlc(1), csdlc-requests(1)."
        ]
      }
    },
    {
      "name": "csdlc-sprint",
      "summary": "inspect sprint readiness from retained issue readbacks",
      "sections": {
        "SYNOPSIS": [
          {
            "code": "csdlc sprint --repo-root ROOT --request REQUEST"
          }
        ],
        "DESCRIPTION": [
          "Read-only helper consumes typed retained issue readbacks, explicit membership and sequential/parallel/hybrid scheduling. It does not authenticate new remote state, bind child worktrees or launch workers. Its retained task-artifact layout is not a substitute for native six-card readiness of current issues."
        ],
        "OPTIONS": [
          "--repo-root: Explicit repository discovery root. Required by foundation/sprint; optional for local-family commands. It does not override authenticated binding guards.",
          "--request: Required path to a UTF-8 JSON request of the command's request family.",
          "--help: Show standalone help; -h is equivalent.",
          "-h: Show standalone help; do not combine with execution options."
        ],
        "REQUEST": [
          "SprintReadinessRequest; see csdlc-requests(1) and docs/csdlc-v3/man/examples/sprint.json."
        ],
        "EFFECTS": [
          "Observation or static model only. Read findings/status, not just process exit status; no release or lifecycle activation authority is granted."
        ],
        "EXAMPLES": [
          {
            "code": "csdlc sprint --repo-root . --request sprint-request.json"
          }
        ],
        "SEE ALSO": [
          "csdlc(1), csdlc-requests(1)."
        ]
      }
    },
    {
      "name": "csdlc-release-preflight",
      "summary": "check exact release-candidate consistency",
      "sections": {
        "SYNOPSIS": [
          {
            "code": "csdlc release-preflight --request REQUEST"
          }
        ],
        "DESCRIPTION": [
          "Checks exact candidate revision, release notes bytes, gate/inventory/evidence digests and package/lockfile consistency. Gate evidence is an input, never release authorization. This route does not tag, publish or mutate GitHub. Git status inspection disables optional locks to avoid index refresh; this observation does not grant release or mutation authority.",
          "The current route is explicitly limited to repository agent-logic/agent-design-language and version v0.92.1; other versions report unsupported_release_identity. Do not claim v0.92.2 release readiness from this older bounded checker."
        ],
        "OPTIONS": [
          "--request: Required path to a UTF-8 JSON request of the command's request family.",
          "--help: Show standalone help; -h is equivalent.",
          "-h: Show standalone help; do not combine with execution options."
        ],
        "REQUEST": [
          "Release preflight Request; see csdlc-requests(1) and docs/csdlc-v3/man/examples/release.json."
        ],
        "EFFECTS": [
          "Observation or static model only. Read findings/status, not just process exit status; no release or lifecycle activation authority is granted."
        ],
        "EXAMPLES": [
          {
            "code": "csdlc release-preflight --request release-preflight-request.json"
          }
        ],
        "SEE ALSO": [
          "csdlc(1), csdlc-requests(1)."
        ]
      }
    },
    {
      "name": "csdlc-requests",
      "summary": "native request fields, enums and digest contracts",
      "sections": {
        "ISSUE INTENT CONTRACT": [
          "Semantic issue intent forms are the current operational contract. Direct issue-writer --request forms are retired and remain available only through --help and --describe for migration discovery. Observation and administrative request forms retain their documented contracts. See docs/csdlc-v3/INTENT_COMMANDS.md for strict content and effect boundaries.",
          "An emitted csdlc.v3.generated_intent_request.v1 wrapper is accepted directly by --intent-request, as is its csdlc.v3.intent_request.v1 request member with schema, command, content, execute, preview and snapshot. No manual extraction is required. Snapshot separates platform, repository/issue, authority selector, native issue generation/digest and checkout root/branch/head. It must equal current identity; stale or foreign-platform requests are refused, never refreshed automatically.",
          "Do not mix --intent-request with positional issue/content/execute/preview arguments. Ordinary argv and the advanced object reach one intent implementation. Existing internal --request schemas below are retained contracts, not interchangeable intent snapshots.",
          "Plan content: csdlc.v3.intent_plan.v1 {schema,slug,cards,validators,publication}; cards contains six current-template values objects, validators declares id/program/args/success_marker and optional timeout_seconds (default 300, admitted 1 through 300), publication declares base/title/body/draft. Changes: csdlc.v3.intent_changes.v1 {schema,cards,amendment}; amendment declares class and transition_approved, with implementation_revision and new_commit when applicable. Publication-only edit uses {schema,publication} with the full base/title/body/draft object, unchanged base, and a line beginning Closes #ISSUE without unrelated closing references; it preserves current candidate proof/review and invalidates publication only. Do not mix cards, validators and publication. Review: csdlc.v3.review_judgment.v1 {schema,implementer,reviewer,reviewed_revision,verdict,evidence}; bookkeeping is tool-owned. Legacy envelopes remain readable. See INTENT_COMMANDS.md for complete field and guard descriptions.",
          "Status decisions content: optional design_ready, dependencies_ready and budget_available booleans; retryable_failure and operator_decision_needed default false. Missing admission decisions stay explicit. Finish disposition content: disposition/operator/rationale/evidence_refs using the native NoPrDisposition enum; authenticated closure timestamps are resolved internally and rechecked by the native owner."
        ],
        "DESCRIPTION": [
          "This is the operator-readable request field reference. Strings are JSON strings, u64 values are nonnegative JSON integers, bool values are JSON booleans, Vec values are arrays and map values are JSON objects. Optional fields may be omitted; a schema-valid shape is only the first gate. Current identity, positive issue IDs where required, authority, receipts, path confinement and operation-specific guards are still mandatory.",
          "The checked-in examples are schema fixtures, not authenticated credentials, approvals, reviews or remote readback. Replace all illustrative IDs/paths/digests from live evidence before execution. Do not copy a fixture's true readiness flags as facts. Request JSON files have no wrapper schema field unless the named type defines one."
        ],
        "LOCAL CONTRACT": [
          "LocalPreparationRequest.commands uses prepare_issue, bind_worktree, edit_cards, plan_pvf, doctor, schedule, shepherd and eligibility. validate requires plan_pvf. The six card_updates keys are sip, stp, spp, vpp, srp and sor; fields come from the active registry's versioned values schemas. No hand-authored rendered cards.",
          "Expected local digest is index.json.digest from the current native state. First issue preparation omits it; bind/edit and subsequent guarded operations use the current value. Registry version must match docs/templates/prompts/current.json. WorktreeRegistration contains actual branch without refs/heads/, normalized absolute worktree and primary boolean; detached checkouts cannot masquerade as issue branches.",
          "schedule_readiness requires every named dimension. shepherd_routing.validation is optional observed validation status, with explicit dependency_wait, retryable_failure, repair_needed and operator_decision_needed booleans. Do not set readiness from desired outcomes."
        ],
        "REMOTE CONTRACT": [
          "RemoteRouteRequest.mode is closing or part_of; readback_source is github, caller or fixture, and only authenticated GitHub evidence qualifies for operational readback. review_present alone is not a review. TypedReviewReceipt.schema is csdlc.v3.typed_review_receipt.v1; repository/issue/head/reviewed_revision must match, reviewer must differ from implementer, and evidence_digest must name actual review evidence.",
          "OperationalRemoteDispatchRequest.operation is {kind:review|publish|github_mutation,request:{...}}. review and publish nest RemoteRouteRequest; github_mutation nests GithubMutationRequest. expected_lifecycle_digest is the payload digest of canonical origin/main authority-selector bytes and exact_review_sha is current reviewed HEAD. Do not substitute the local issue digest.",
          "GithubMutationRequest.mutation is tagged by action. issue_create requires title/body and optionally labels, assignees, milestone. issue_comment requires body. issue_edit optionally supplies title/body and explicit metadata operations documented by csdlc-github-issue(1). issue_close requires rationale/current_body/disposition (duplicate|superseded|no_op), duplicate_of for duplicate, and only not_planned for github_state_reason.",
          "pull_request_create requires base/head/title/body and optional draft boolean. head is the actual branch name and expected_head_sha is its exact reviewed SHA. pull_request_update supplies title/body. pull_request_ready has no payload fields. pull_request_merge requires base, method merge, review_receipt_path, review_receipt_digest and operator_approval in the enclosing request. Only github-pr owns merge.",
          "recovery may be retry_after_authenticated_absence for supported creation/ready cases; it is not generic retry permission and cannot replay merge or issue-edit writes. cutover_issue/operator_approval do not grant authority by themselves."
        ],
        "DIGESTS": [
          "Digest algorithms are field-specific. Artifact/terminal byte digests use BLAKE3 of the exact bytes. Remote stable payload digests use BLAKE3 over each ordered UTF-8 field followed by a NUL byte. Canonical authority selector payload digest is BLAKE3 of origin/main:csdlc-v3/operator/authority-selector.json bytes followed by NUL. Whitespace in those bytes matters.",
          "Typed review payload field order: schema, repository, issue rendered as decimal, implementer, reviewer, reviewed_revision, expected_head_sha, evidence_digest; append NUL after each. The typed receipt file itself need not have the same byte digest. Use the supplied fixture-checked operator digest example rather than confusing SHA-256 or a file hash with this payload digest.",
          "A native result's returned digest/receipt path is preferable to recomputing a lifecycle receipt. Never claim authentication by writing a receipt with a matching checksum; transport-derived receipts require actual native observation."
        ],
        "TERMINAL CONTRACT": [
          "TerminalRouteRequest.mode is closing or part_of. Ordinary finish needs numeric pull_request, expected_head_sha and closing mode plus --observe-github. publication_repository optionally identifies a different owner/name repository containing the closing pull request; authenticated readback must still prove that it closes the target issue in repository, and the durable receipt retains both identities. historical_pull_requests is a finish-only list of retained checkpoint PRs: each must authenticate as merged for the publication repository and issue, must not close the issue, and must explicitly declare Part of or Part-Of linkage; it does not override the selected closing PR. terminal_state identifies the canonical repository_root and exact state_path/receipt_path; optional reconciliation_checkout binds the checkout whose HEAD must match a no-PR expected head, while optional expected_state_digest binds existing terminal bytes. Omitting terminal_state performs observation without durable terminal persistence.",
          "NoPrCloseout.disposition is retired_without_execution, duplicate, superseded, absorbed, coordination_completed or historical_disposition. It requires operator/rationale/evidence_refs and exact authenticated issue updated/closed timestamps. It is administrative disposition, not merged implementation. expected_head_sha must match the actual reconciliation checkout for this route.",
          "CleanupRouteRequest.remove defaults false. terminal_receipt is not sufficient alone: use terminal_receipt_path and its exact BLAKE3 byte digest, then preview_receipt_digest returned by an eligible preview for removal. retained_archive_identity is tool-owned compatibility state retained from preview and is not operator authority. approved_parent/repository_root/candidate_path must be actual normalized paths; candidate must be absolute. Preserve dirty or live work.",
          "CutoverDecisionRequest.operation is apply (default) or rollback; execute defaults false. Execution requires exact selected binary, selector, installation, rollback receipt and readiness evidence paths/digests plus explicit approval. Retained cutover configuration is not a routine startup requirement."
        ],
        "PROOF CONTRACT": [
          "ProofWorktreeBinding compares worktree, branch, exact_head, git_common_dir, generation and lifecycle_digest with the actual native-bound issue. evidence_root is that checkout. ProofManifest.normalization is doctor_issue_phase_v1 or route_preview_v1; command.generation is v3 (v2 is retained historical input only). provider_side_effects must be false for this bounded proof.",
          "ShadowCommandSpec.binary_ref/request_ref/side_effect_boundary_refs are confined artifact references, argv is a literal argument array, and timeout_millis is an explicit bound. No shell expansion occurs. doctor_issue_phase_v1 normalizes a real doctor response; route_preview_v1 expects the exact typed semantic preview contract, not arbitrary help text. The current CLI has no --preview producer, so routine examples use doctor_issue_phase_v1; do not invent a successful preview invocation.",
          "InstallPlanInput requires verified source artifact/provenance/selector references and digests. stable_destination must be truthful. executes_install true requires destination .adl/bin/csdlc, exact head and matching explicit cutover_approval_ref/digest. ShadowComparison and SoakEvidence remain historical parseable shapes; the public shadow/soak execution routes are disabled."
        ],
        "SPRINT AND RELEASE": [
          "SprintReadinessRequest.sprints contains SprintReadinessTarget entries. execution_mode is sequential, parallel or hybrid. child_readback_refs maps issue-number strings to retained typed issue readback files; umbrella_readback_ref identifies the umbrella readback. Paths are bounded repository references. This retained helper does not prove current native cards merely by accepting readbacks.",
          "ReleaseRequest carries exact repository/version/candidate_sha, notes_path/notes_digest and gate_path/gate_digest. The gate schema is csdlc.v3.release_gate.v1, with repository/version/candidate_sha/notes_path/notes_digest/inventory_digest/status status must be ready_for_preflight, and evidence [{issue,path,digest}]. Evidence and inventories are versioned repository inputs, not caller release authorization."
        ],
        "LOCALPREPARATIONREQUEST": [
          "issue: u64; required.",
          "title: String; required.",
          "repository: String; required.",
          "branch: String; required.",
          "worktree: String; required.",
          "registry_version: String; required.",
          "expected_lifecycle_digest: Option<String>; optional/defaulted.",
          "commands: Vec<LocalCommand>; required.",
          "card_updates: BTreeMap<String, Value>; optional/defaulted.",
          "schedule_readiness: Option<ScheduleReadinessInput>; optional/defaulted.",
          "shepherd_routing: Option<ShepherdRoutingInput>; optional/defaulted."
        ],
        "SCHEDULEREADINESSINPUT": [
          "phase_ready: bool; required.",
          "cards_ready: bool; required.",
          "design_ready: bool; required.",
          "dependencies_ready: bool; required.",
          "paths_clear: bool; required.",
          "budget_available: bool; required."
        ],
        "SHEPHERDROUTINGINPUT": [
          "validation: Option<String>; optional/defaulted.",
          "dependency_wait: bool; optional/defaulted.",
          "retryable_failure: bool; optional/defaulted.",
          "repair_needed: bool; optional/defaulted.",
          "operator_decision_needed: bool; optional/defaulted."
        ],
        "WORKTREEREGISTRATION": [
          "branch: String; required.",
          "worktree: String; required.",
          "primary: bool; required."
        ],
        "REMOTEROUTEREQUEST": [
          "repository: String; required.",
          "issue: u64; required.",
          "pull_request: Option<u64>; optional/defaulted.",
          "actor: Option<String>; optional/defaulted.",
          "implementer: Option<String>; optional/defaulted.",
          "reviewer: Option<String>; optional/defaulted.",
          "review_revision: Option<String>; optional/defaulted.",
          "expected_head_sha: Option<String>; optional/defaulted.",
          "head_sha: Option<String>; optional/defaulted.",
          "mode: Option<RemotePublicationMode>; optional/defaulted.",
          "title: Option<String>; optional/defaulted.",
          "body: Option<String>; optional/defaulted.",
          "review_present: bool; optional/defaulted.",
          "typed_review_receipt_path: Option<String>; optional/defaulted.",
          "typed_review_receipt_digest: Option<String>; optional/defaulted.",
          "readback_source: Option<RemoteReadbackSource>; optional/defaulted.",
          "readback_receipt_path: Option<String>; optional/defaulted.",
          "readback_receipt_digest: Option<String>; optional/defaulted.",
          "adapter_receipt_path: Option<String>; optional/defaulted.",
          "adapter_receipt_digest: Option<String>; optional/defaulted.",
          "closes_issue: Option<u64>; optional/defaulted.",
          "closing_issues: Vec<u64>; optional/defaulted.",
          "part_of_issue: Option<u64>; optional/defaulted.",
          "credential_names: Vec<String>; optional/defaulted."
        ],
        "TYPEDREVIEWRECEIPT": [
          "schema: String; required.",
          "repository: String; required.",
          "issue: u64; required.",
          "implementer: String; required.",
          "reviewer: String; required.",
          "reviewed_revision: String; required.",
          "expected_head_sha: String; required.",
          "evidence_digest: String; required.",
          "publication_linkage: Option<PublicationLinkage>; required for merge, optional for publication-only compatibility. Its qualified repository, issue and mode enter the typed review payload digest. A pre-linkage merge receipt cannot authorize a new merge."
        ],
        "GITHUBREADBACKRECEIPT": [
          "schema: String; required.",
          "repository: String; required.",
          "issue: u64; required.",
          "pull_request: u64; required.",
          "title: Option<String>; optional/defaulted.",
          "head_sha: String; required.",
          "closes_issue: Option<u64>; optional/defaulted.",
          "closing_issues: Vec<u64>; optional/defaulted.",
          "part_of_issue: Option<u64>; optional/defaulted.",
          "source: RemoteReadbackSource; required.",
          "observed_by: String; required."
        ],
        "GITHUBADAPTERRECEIPT": [
          "schema: String; required.",
          "repository: String; required.",
          "issue: u64; required.",
          "pull_request: u64; required.",
          "head_sha: String; required.",
          "readback_receipt_digest: String; required.",
          "credential_names: Vec<String>; required.",
          "adapter: String; required.",
          "authenticated: bool; required."
        ],
        "GITHUBMUTATIONREQUEST": [
          "repository: String; required.",
          "issue: u64; optional/defaulted.",
          "pull_request: Option<u64>; optional/defaulted.",
          "cutover_issue: Option<u64>; optional/defaulted.",
          "operator_approval: Option<String>; optional/defaulted.",
          "expected_head_sha: String; required.",
          "credential_names: Vec<String>; required.",
          "recovery: Option<GithubMutationRecovery>; optional/defaulted.",
          "mutation: GithubMutation; required."
        ],
        "OPERATIONALREMOTEDISPATCHREQUEST": [
          "expected_lifecycle_digest: String; required.",
          "exact_review_sha: String; required.",
          "operation: OperationalRemoteOperation; required."
        ],
        "TERMINALROUTEREQUEST": [
          "repository: String; required.",
          "publication_repository: Option<String>; optional/defaulted.",
          "issue: u64; required.",
          "pull_request: Option<u64>; optional/defaulted.",
          "historical_pull_requests: Vec<u64>; optional/defaulted.",
          "expected_head_sha: Option<String>; optional/defaulted.",
          "mode: Option<TerminalPublicationMode>; optional/defaulted.",
          "public_adapter_receipt: Option<AdapterReceipt>; optional/defaulted.",
          "terminal_state: Option<TerminalStateWriteRequest>; optional/defaulted.",
          "no_pr_closeout: Option<NoPrCloseout>; optional/defaulted.",
          "cleanup: Option<CleanupRouteRequest>; optional/defaulted.",
          "cutover: Option<CutoverDecisionRequest>; optional/defaulted.",
          "credential_names: Vec<String>; optional/defaulted."
        ],
        "NOPRCLOSEOUT": [
          "disposition: NoPrDisposition; required.",
          "operator: String; required.",
          "rationale: String; required.",
          "evidence_refs: Vec<String>; required.",
          "expected_issue_updated_at: String; required.",
          "expected_issue_closed_at: String; required."
        ],
        "ADAPTERRECEIPT": [
          "producer: String; required.",
          "digest: String; required."
        ],
        "CLEANUPROUTEREQUEST": [
          "approved_parent: PathBuf; required.",
          "repository_root: PathBuf; required.",
          "candidate_path: PathBuf; required.",
          "remove: bool; optional/defaulted.",
          "terminal_receipt: bool; optional/defaulted.",
          "terminal_receipt_path: Option<String>; optional/defaulted.",
          "terminal_receipt_digest: Option<String>; optional/defaulted.",
          "preview_receipt_digest: Option<String>; optional/defaulted.",
          "retained_archive_identity: Option<serde_json::Value>; optional/defaulted."
        ],
        "TERMINALSTATEWRITEREQUEST": [
          "repository_root: PathBuf; required.",
          "reconciliation_checkout: Option<PathBuf>; optional/defaulted.",
          "state_path: PathBuf; required.",
          "receipt_path: PathBuf; required.",
          "expected_state_digest: Option<String>; optional/defaulted."
        ],
        "CUTOVERDECISIONREQUEST": [
          "operator: String; required.",
          "approval: String; required.",
          "selected_binary_provenance: String; required.",
          "rollback_evidence: String; required.",
          "undo_boundary: String; required.",
          "operation: CutoverOperation; optional/defaulted.",
          "execute: bool; optional/defaulted.",
          "repository_root: Option<PathBuf>; optional/defaulted.",
          "selected_binary_path: Option<PathBuf>; optional/defaulted.",
          "authority_selector_path: Option<PathBuf>; optional/defaulted.",
          "install_destination_path: Option<PathBuf>; optional/defaulted.",
          "rollback_receipt_path: Option<PathBuf>; optional/defaulted.",
          "readiness_evidence_path: Option<PathBuf>; optional/defaulted.",
          "readiness_evidence_digest: Option<String>; optional/defaulted."
        ],
        "PROOFROUTEREQUEST": [
          "issue: u64; required.",
          "repository: String; required.",
          "binding: Option<ProofWorktreeBinding>; optional/defaulted.",
          "cutover_issue: Option<u64>; optional/defaulted.",
          "operator_approval: Option<String>; optional/defaulted.",
          "evidence_root: Option<String>; optional/defaulted.",
          "proof: Option<ProofManifest>; optional/defaulted.",
          "shadow: Option<ShadowComparison>; optional/defaulted.",
          "soak: Option<SoakEvidence>; optional/defaulted.",
          "install: Option<InstallPlanInput>; optional/defaulted."
        ],
        "PROOFWORKTREEBINDING": [
          "worktree: PathBuf; required.",
          "branch: String; required.",
          "exact_head: String; required.",
          "git_common_dir: PathBuf; required.",
          "generation: u64; required.",
          "lifecycle_digest: String; required."
        ],
        "PROOFMANIFEST": [
          "manifest_id: String; required.",
          "lane: String; required.",
          "deterministic: bool; required.",
          "evidence_ref: String; required.",
          "evidence_digest: String; required.",
          "observed_digest: String; required.",
          "stale: bool; required.",
          "normalization: ShadowNormalizationContract; required.",
          "command: ShadowCommandSpec; required."
        ],
        "SHADOWCOMPARISON": [
          "normalization: ShadowNormalizationContract; required.",
          "v2: ShadowCommandSpec; required.",
          "v3: ShadowCommandSpec; required.",
          "broad_equivalence_claim: bool; required."
        ],
        "SOAKEVIDENCE": [
          "normalization: ShadowNormalizationContract; required.",
          "command: ShadowCommandSpec; required.",
          "duration_millis: u64; required.",
          "sample_interval_millis: u64; required.",
          "hidden_state: bool; required.",
          "provider_side_effects: bool; required."
        ],
        "SHADOWCOMMANDSPEC": [
          "generation: ShadowGeneration; required.",
          "binary_ref: String; required.",
          "argv: Vec<String>; required.",
          "request_ref: String; required.",
          "timeout_millis: u64; required.",
          "side_effect_boundary_refs: Vec<String>; required.",
          "provider_side_effects: bool; required."
        ],
        "INSTALLPLANINPUT": [
          "artifact_name: String; required.",
          "artifact_ref: String; required.",
          "source_provenance_ref: String; required.",
          "selector_metadata_ref: String; required.",
          "source_provenance: String; required.",
          "selected_binary_digest: String; required.",
          "observed_binary_digest: String; required.",
          "selector_metadata_digest: String; required.",
          "destination: String; required.",
          "stable_destination: bool; required.",
          "executes_install: bool; required.",
          "exact_head: String; optional/defaulted.",
          "cutover_approval_ref: Option<String>; optional/defaulted.",
          "cutover_approval_digest: Option<String>; optional/defaulted."
        ],
        "SPRINTREADINESSREQUEST": [
          "repository: String; required.",
          "version: String; required.",
          "sprints: Vec<SprintReadinessTarget>; required."
        ],
        "SPRINTREADINESSTARGET": [
          "sprint: u64; required.",
          "umbrella_issue: u64; required.",
          "title: String; required.",
          "execution_mode: SprintExecutionMode; required.",
          "serial_gates: Vec<String>; required.",
          "umbrella_readback_ref: String; required.",
          "child_readback_refs: BTreeMap<String, String>; required."
        ],
        "RELEASEREQUEST": [
          "repository: String; required.",
          "version: String; required.",
          "candidate_sha: String; required.",
          "notes_path: String; required.",
          "notes_digest: String; required.",
          "gate_path: String; required.",
          "gate_digest: String; required."
        ],
        "PUBLICATIONLINKAGE": [
          "repository: String; required qualified owner/repository of the issue, which may differ from the PR code repository.",
          "issue: u64; required positive issue number matching the merge request.",
          "mode: RemotePublicationMode; required closing or part_of. The reviewed mode controls both authenticated pre-dispatch relation and post-merge issue state."
        ],
        "VALIDATOR": [
          "Admitted Cargo test declaration. timeout_seconds is optional at input, defaults to 300 and must be 1 through 300; its resolved value is bound into the validator input digest and actual receipt.",
          "id: String",
          "program: String",
          "args: Vec<String>",
          "success_marker: String",
          "timeout_seconds: u64"
        ],
        "PUBLICATION": [
          "Default publication plan. Explicit GitHub update content can change title/body without rewriting this plan; all closing linkage and exact candidate guards remain required.",
          "base: String",
          "title: String",
          "body: String",
          "draft: bool"
        ],
        "INTENTPLAN": [
          "Strict preparation plan with six active-template values objects and declared validator/publication intent; identity comes from the owner.",
          "schema: String",
          "slug: String",
          "cards: BTreeMap<String, Value>",
          "validators: Vec<Validator>",
          "publication: Publication"
        ],
        "INTENTREQUEST": [
          "Strict shared advanced request. The complete emitted wrapper or this inner object is accepted; current platform/authority/issue-version/checkout snapshot must agree before execution.",
          "schema: String",
          "command: String",
          "content: Value",
          "execute: bool",
          "preview: Option<String>",
          "snapshot: Snapshot"
        ]
      }
    },
    {
      "name": "csdlc-workflow",
      "summary": "prepare, execute, publish and reconcile one issue",
      "sections": {
        "CANDIDATE ISSUE INTENT SEQUENCE": [
          "Semantic issue intent forms are the current operational contract. Direct issue-writer --request forms are retired and remain available only through --help and --describe for migration discovery. Observation and administrative request forms retain their documented contracts. See docs/csdlc-v3/INTENT_COMMANDS.md for strict content and effect boundaries.",
          {
            "code": "csdlc status ISSUE\ncsdlc prepare ISSUE --plan PLAN.json\ncsdlc bind ISSUE\ncsdlc edit ISSUE --changes CHANGES.json\ncsdlc validate ISSUE\ncsdlc proof ISSUE\ncsdlc review ISSUE --evidence REVIEW.json\ncsdlc publish ISSUE\n# Merge only after explicit exact-target authorization and native admission.\ncsdlc finish ISSUE\ncsdlc clean ISSUE\ncsdlc clean ISSUE --execute --preview TOKEN"
          },
          "Supply issue-specific six-card values from the active templates, admitted validators and canonical publication content. Independent review is external evidence, never an automatically generated approval. Finish requires already merged/closed remote truth; cleanup is a separate token-bound operation. Direct writer request syntax is retired; use positional intents or the declared intent request contract. Read-only diagnostic requests remain supported."
        ],
        "PRECONDITIONS": [
          "Use an existing tracked issue, accepted prerequisites and a reviewed issue-specific plan. Check clean primary main and actual registrations; preserve another session's checkout. Commands below are templates using your own reviewed request files, not permission to mutate example IDs. Use the active installed binary by absolute path after changing checkouts.",
          "Optional documentation helper: cargo run --quiet --manifest-path csdlc-v3/Cargo.toml --example operator_manual -- ACTION PATH. Actions selector-digest, review-digest, file-digest and registrations only read and print. It requires the pinned Rust toolchain/dependencies and is not installed as a lifecycle owner. It calls the current native public digest functions; it does not authenticate authored receipts."
        ],
        "1 PREPARE": [
          {
            "code": "git status --short --branch\ngit worktree list --porcelain\ncsdlc prepare ISSUE --plan plan.json\ncsdlc validate ISSUE\ncsdlc doctor --request current.json --registry docs/templates/prompts/current.json --registrations registrations.json"
          },
          "Store invocation files under resolved primary Git metadata csdlc-v3/local/invocations/ISSUE. plan.json uses schema csdlc.v3.intent_plan.v1 with slug, all six cards (sip, stp, spp, vpp, srp, sor), validators and publication. Supply issue-specific values from the active template registry. The owner derives issue identity and approved branch/worktree from current authenticated context; do not author LocalPreparationRequest for prepare --plan.",
          "After binding, design-edit.json uses schema csdlc.v3.intent_changes.v1, cards keyed by card name, and amendment with class and transition_approved. Replace validator declarations with validators instead of cards; change publication metadata with publication alone. Select exactly one edit surface. Do not send card_updates or expected_lifecycle_digest: those belong to the native compatibility request, not this semantic command. Preserve SIP/STP/SPP design truth, select a proportional VPP lane, and leave review/output completion unclaimed. See docs/csdlc-v3/INTENT_COMMANDS.md for full values and admission constraints; --emit-request captures a current immutable snapshot when explicit replay is needed.",
          {
            "code": "{\n  \"schema\": \"csdlc.v3.intent_changes.v1\",\n  \"cards\": {\n    \"spp\": {\n      \"notes_risks_inline\": \"Reviewed issue-local plan; implementation and proof remain pending.\"\n    }\n  },\n  \"amendment\": {\n    \"class\": \"plan\",\n    \"transition_approved\": true\n  }\n}"
          },
          "Generate registrations.json from actual Git topology using the optional registrations helper. Read every returned finding. A schema-valid generic template is not an accepted design.",
          "An issue with no native record uses the same prepare command and a complete six-card plan. For a retained pre-semantic native-v3 record, prepare is an explicit compatibility operation: the owner fences the exact issue writer and accepts either a structurally complete unbound ready record or an already-bound record whose common and linked bindings, registered branch, worktree, current head, generation, digest, cards and completion chain all agree. Pending, damaged, ambiguous or differently owned state is rejected without mutation. Every retained source byte remains in place, and an interrupted activation converges through an identical prepare replay. Before semantic preparation, status and edit may report intent_semantic_migration_required; after successful preparation, status separately reports card structure, dependency readiness and execution readiness. Existing-issue GitHub mutation previews apply the same semantic admission as execution and therefore expose missing preparation instead of reporting ready."
        ],
        "2 BIND": [
          {
            "code": "csdlc bind ISSUE\ncd /absolute/approved/issue-worktree\ncsdlc edit ISSUE --changes design-edit.json\ncsdlc validate ISSUE\ncsdlc doctor --request current.json --registry docs/templates/prompts/current.json --registrations registrations.json"
          },
          "Verify the exact native bound branch, returned worktree, phase and six cards. Refresh current.json from the worktree .csdlc/issues/ISSUE/index.json and refresh registrations after bind. Use an absolute installed binary path or configured PATH. Create the issue-bound session goal before implementation; keep root main inspection-only."
        ],
        "3 IMPLEMENT AND REVIEW": [
          "Implement only planned paths, execute VPP's smallest meaningful proof, update SPP if the plan changes and SOR with actual results through native edit/validate. Commit the intended source and evidence so review binds an exact head. Obtain independent review at that exact revision; resolve actionable findings and renew proof/review after source changes.",
          "Record one actual independent review judgment using csdlc.v3.review_judgment.v1: implementer, reviewer, reviewed_revision, verdict and evidence. The tool derives receipt and proof digests. PR metadata changes preserve current candidate validation and review; scope, validator and source changes still require appropriate fresh evidence.",
          {
            "code": "csdlc review ISSUE --evidence review-evidence.json"
          },
          "The ordinary issue review command derives the native request and retains judgment plus receipt together. No manual dispatch wrapper or digest helper is needed. Historical native receipts remain readable."
        ],
        "4 CREATE AND VERIFY THE PR": [
          {
            "code": "csdlc review ISSUE --evidence REVIEW.json\ngit push -u origin YOUR_REVIEWED_BRANCH\ncsdlc publish ISSUE\ncsdlc pr-state ISSUE"
          },
          "Push the exact reviewed branch before publication. Native publication verifies the authenticated remote branch head and does not push Git refs; a missing or changed remote head blocks dispatch.",
          "Publication resolves the exact reviewed head and accepted base, title, body and draft metadata. Correct metadata with csdlc edit ISSUE --changes publication.json before renewed proof and independent review. The changes use schema csdlc.v3.intent_changes.v1 and one publication object with base, title, body and draft. Closes #ISSUE must begin a line. Preparation rejects malformed metadata. Do not use retired github-pr --request writer syntax.",
          "Metadata amendments preserve issue and binding identity and current candidate validation/review. Publication state is invalidated. Older proof records without candidate-input identity need fresh proof after an input-version change. After publication, base and draft cannot change through this editor. Verify actual base/head and closing linkage immediately after publication.",
          "To transition from draft after required proof, use csdlc github-pr ISSUE --operation ready-operation.json --execute with action pull_request_ready. A published PR remains open/unmerged until explicitly authorized merge."
        ],
        "5 MERGE ONLY WITH AUTHORIZATION": [
          "Confirm required checks actually pass, unresolved review threads are resolved, current review is exact and the operator authorized this numeric PR merge. Use mutation pull_request_merge only through github-pr with explicit base, method merge and current review receipt path/digest. Put the actual authorization reference in operator_approval; text alone is not approval.",
          {
            "code": "csdlc github-pr ISSUE --operation merge-operation.json --execute"
          },
          "Retain authenticated reconciliation. Unsupported branch policy, incomplete observations or head/base/check drift stay blocked. All retries after merge intent are observation-only; do not delete the guard or send a second raw merge."
        ],
        "6 FINISH": [
          {
            "code": "csdlc finish ISSUE"
          },
          "Use TerminalRouteRequest repository/issue/numeric PR/expected_head_sha/mode closing and credential_names. When retained earlier publication targets exist, list them in historical_pull_requests so finish authenticates each merged non-closing Part of checkpoint before accepting the selected closing PR. To persist terminal truth include terminal_state with the canonical repository_root and exact state/receipt locations from csdlc(1); for no-PR reconciliation, reconciliation_checkout may name the bound checkout whose HEAD must match expected_head_sha without changing the canonical output root. Linked: .csdlc/v3/issues/ISSUE/terminal.json and .csdlc/evidence/ISSUE/terminal-receipt.json. Primary: resolved Git metadata csdlc-v3/local/v3/issues/ISSUE/terminal.json and csdlc-v3/local/evidence/ISSUE/terminal-receipt.json.",
          "Finish independently observes merged PR, correct expected head and closed linked issue; no second GitHub mutation or closeout PR is needed. Retain returned terminal state/receipt and distinguish terminal observation from persistence when terminal_state is omitted."
        ],
        "7 CLEAN": [
          {
            "code": "csdlc clean ISSUE\ncsdlc clean ISSUE --execute --preview TOKEN"
          },
          "Before cleaning from primary, repeat the authenticated finish invocation from primary with terminal_state.repository_root set to that primary checkout and primary canonical Git-metadata state/receipt paths. The linked finish receipt in step 6 is not a primary cleanup receipt: do not copy it or point primary cleanup at the linked .csdlc file. Retain the newly authenticated primary receipt and its byte digest.",
          "Run clean from the primary, not the target you will remove. Supply the actual approved parent, primary repository root, exact registered candidate, expected head and that primary durable terminal receipt path/digest. First remove false; only a Removable result supplies the preview receipt for a second request with remove true and matching preview_receipt_digest.",
          "Preserve dirty, live or unregistered work. The cleanup gate checks a live-worktree marker and Git state; it is not a general host process scanner. Remove only the exact target and verify the returned Removed result. Do not broaden cleanup to other issue worktrees."
        ],
        "RECOVERY": [
          "Local compare-and-swap drift: inspect actual current state and regenerate only the intended semantic edit with fresh digest. Interrupted transaction: retain original request and state, use the identical native mutator request, and route unresolved failures to bounded tooling repair. No handwritten index/journal recovery.",
          "Remote uncertain response: same exact request, same target and retained intent; native reconciliation observes the remote effect. An uncertain result is not a successful publication, merge or finish. Read csdlc-github-pr(1) for merge-specific one-shot dispatch semantics.",
          "This manual's schema fixtures prove request shape and selected guard/plan behavior. They do not claim an end-to-end live GitHub deployment or authorize example targets."
        ]
      }
    },
    {
      "name": "csdlc-status",
      "summary": "observe one issue without running its validators",
      "sections": {
        "SYNOPSIS": [
          {
            "code": "csdlc status ISSUE [--decisions DECISIONS.json] [--repo-root ROOT] [--json]\ncsdlc status ISSUE --emit-request\ncsdlc status --intent-request REQUEST.json"
          }
        ],
        "DESCRIPTION": [
          "Semantic issue intent forms are the current operational contract. Direct issue-writer --request forms are retired and remain available only through --help and --describe for migration discovery. Observation and administrative request forms retain their documented contracts. See docs/csdlc-v3/INTENT_COMMANDS.md for strict content and effect boundaries.",
          "Resolve the canonical issue, bound checkout, authority and version; call the existing doctor, eligibility, schedule and shepherd owners. Report actual validation findings, proof currency and allowed next intents without executing validators.",
          "Dependency completion and budget acceptance remain explicit operator decisions. A report is not authorization to begin a later sprint child. Missing or ambiguous identity fails closed; observation does not replay a pending transaction.",
          "The generated wrapper emitted by --emit-request can be passed directly to --intent-request; the inner request is also accepted.",
          "Status invokes doctor, eligibility, schedule and shepherd observation owners. Optional --decisions contains strict optional design_ready/dependencies_ready/budget_available booleans plus retryable_failure and operator_decision_needed booleans defaulting false. Absent design/dependency/budget decisions require explicit operator input, never inferred completion."
        ],
        "OUTPUT": [
          "Machine JSON uses the common command-result envelope. Interpret status, authority, effects and findings independently. An emitted request is a snapshot, not execution. Foreign-platform snapshot rejection is not non-Unix execution proof."
        ],
        "SEE ALSO": [
          "csdlc(1), csdlc-requests(1), csdlc-workflow(1), docs/csdlc-v3/INTENT_COMMANDS.md."
        ],
        "OPTIONS": [
          "Supported candidate intent options: --decisions, --emit-request, --help, --intent-request, --json, --repo-root, -h. Positional ISSUE is positive; --help and -h are standalone discovery."
        ],
        "EXAMPLES": [
          {
            "code": "csdlc status ISSUE [--repo-root ROOT] [--json]\ncsdlc status ISSUE --emit-request\ncsdlc status --intent-request REQUEST.json"
          },
          "These forms require a supporting selected candidate; they do not upgrade the active binary or authorize activation."
        ]
      }
    },
    {
      "name": "csdlc-prepare",
      "summary": "prepare an authenticated issue from a declared plan",
      "sections": {
        "SYNOPSIS": [
          {
            "code": "csdlc prepare ISSUE --plan PLAN.json [--repo-root ROOT] [--json]\ncsdlc prepare ISSUE --plan PLAN.json --emit-request\ncsdlc prepare --intent-request REQUEST.json"
          }
        ],
        "DESCRIPTION": [
          "Semantic issue intent forms are the current operational contract. Direct issue-writer --request forms are retired and remain available only through --help and --describe for migration discovery. Observation and administrative request forms retain their documented contracts. See docs/csdlc-v3/INTENT_COMMANDS.md for strict content and effect boundaries.",
          "Authenticate the existing source issue and resolve title/repository/worktree policy and template registry. Prepare the strict csdlc.v3.intent_plan.v1 through the local owner, retaining its plan atomically with native issue initialization.",
          "Plan fields are schema, slug, cards, validators and publication. Cards contains all six active-template values objects. Validators declare bounded Cargo tests or preparation-only checks: python3 with a tracked relative .py script and optional --self-test, git diff --check, or manual-review with one safe scope identifier. Publication declares base/title/body/draft and the canonical closing relation. Identity fields are derived by the owner and cannot be overridden by card prose.",
          "Non-Cargo declarations are not executable proof adapters. Proof refuses intent_validator_execution_unsupported before effects if any are present; preparation does not establish review, product acceptance or publication admission. Unsupported execution must not be replaced by unrelated passing Rust tests.",
          "The command writes preparation state in resolved primary Git metadata. It does not bind the issue worktree, execute proof, publish or install the candidate. Existing initialization conflicts or interrupted transactions are refused for explicit recovery. Preview/execute overrides are unsupported.",
          "The generated wrapper emitted by --emit-request can be passed directly to --intent-request; the inner request is also accepted."
        ],
        "OUTPUT": [
          "Machine JSON uses the common command-result envelope. Interpret status, authority, effects and findings independently. An emitted request is a snapshot, not execution. Foreign-platform snapshot rejection is not non-Unix execution proof."
        ],
        "SEE ALSO": [
          "csdlc(1), csdlc-requests(1), csdlc-workflow(1), docs/csdlc-v3/INTENT_COMMANDS.md."
        ],
        "OPTIONS": [
          "Supported candidate intent options: --emit-request, --help, --intent-request, --json, --plan, --repo-root, -h. Positional ISSUE is positive; --help and -h are standalone discovery. Unsupported combinations fail closed."
        ],
        "EXAMPLES": [
          {
            "code": "csdlc prepare ISSUE --plan PLAN.json [--repo-root ROOT] [--json]\ncsdlc prepare ISSUE --plan PLAN.json --emit-request\ncsdlc prepare --intent-request REQUEST.json"
          },
          "These forms require a supporting selected candidate; they do not upgrade the active binary or authorize activation."
        ]
      }
    },
    {
      "name": "csdlc-rebuild",
      "summary": "explicitly rebuild all six cards from the semantic issue record",
      "sections": {
        "SYNOPSIS": [
          {
            "code": "csdlc rebuild ISSUE [--repo-root ROOT] [--json]\ncsdlc rebuild ISSUE --emit-request\ncsdlc rebuild --intent-request REQUEST.json"
          }
        ],
        "DESCRIPTION": [
          "Semantic issue intent forms are the current operational contract. Rebuild resolves the authenticated issue, bound checkout, active prompt registry and exact semantic source version before any write.",
          "Derive all six SIP, STP, SPP, VPP, SRP and SOR values and rendered cards from the single semantic issue record. Write projection artifacts deterministically and publish the manifest last. Regeneration does not write a lifecycle acknowledgement or advance the task version.",
          "Status and validate report projection health without writing or replacing the task next step. Rebuild regenerates missing, altered or interrupted views directly. An unchanged healthy projection is an expected no-op with identical bytes and digests.",
          "Rebuild preserves semantic facts, evidence references and immutable receipt bytes. It cannot create approval, proof, accepted review findings, publication or terminal completion. Pending work, stale semantic identity, corrupted retained state and mismatched issue or checkout fail closed.",
          "The generated wrapper emitted by --emit-request can be passed directly to --intent-request; its semantic_version prevents a reviewed request from silently rebuilding a later semantic record."
        ],
        "OUTPUT": [
          "Machine JSON uses the common command-result envelope and reports projection health, semantic digest, projection digest, generated paths, mutation truth and semantic version."
        ],
        "RECOVERY": [
          "If projection writing is interrupted, ordinary inspection remains read-only. Rebuild can resume recognized generated staging residue directly; corrupt or unknown residue remains an explicit failure. Do not hand-edit partial output. The older recover preview/execution interface remains available for retained requests."
        ],
        "SEE ALSO": [
          "csdlc(1), csdlc-status(1), csdlc-validate(1), csdlc-recover(1), csdlc-workflow(1), docs/csdlc-v3/INTENT_COMMANDS.md."
        ],
        "OPTIONS": [
          "Supported intent options: --emit-request, --help, --intent-request, --json, --repo-root, -h. Positional ISSUE is positive; unsupported content, preview and execute arguments fail closed."
        ],
        "EXAMPLES": [
          {
            "code": "csdlc status ISSUE\ncsdlc rebuild ISSUE\ncsdlc validate ISSUE"
          },
          "Use an isolated selected candidate for qualification. This operation does not authorize live conversion, writer activation or shared binary replacement."
        ]
      }
    },
    {
      "name": "csdlc-recover",
      "summary": "preview and execute recovery of one retained native operation",
      "sections": {
        "SYNOPSIS": [
          {
            "code": "csdlc recover ISSUE [--repo-root ROOT] [--json]\ncsdlc recover ISSUE --execute --preview DIGEST\ncsdlc recover ISSUE --disposition DISPOSITION.json --execute --preview DIGEST\ncsdlc recover --intent-request REQUEST.json"
          }
        ],
        "DESCRIPTION": [
          "Semantic issue intent forms are the current operational contract. Direct issue-writer --request forms are retired and remain available only through --help and --describe for migration discovery. Observation and administrative request forms retain their documented contracts. See docs/csdlc-v3/INTENT_COMMANDS.md for strict content and effect boundaries.",
          "Preview retained issue journals, pending Finish/FinishWithoutPr, native local transactions or original remote mutations and return preview_digest without token files or mutation. Ordinary issue recovery needs no extra operation input; repository-scoped creation journal activation requires explicit --disposition selection.",
          "For a creation journal, --disposition contains only schema csdlc.v3.creation_journal_recovery_disposition.v1 and operation_id from the retained semantic operation. Preview with this selection, then repeat it with --execute --preview DIGEST. Authentication binds retained effective authority and checkout head, including any prior authenticated recovery adoption; unadopted changes, corrupt bytes or stale preview fail closed. Only the retained commit is activated; no business effect is replayed. See INTENT_COMMANDS.md for the exact object and follow-up reconciliation.",
          "Execution requires the exact current preview token. Local recovery holds the native issue lock and compares its current transaction identity. Remote recovery binds the issue snapshot and complete retained native intent bytes, then reconciles the original request without recomputing its operation.",
          "If a proof attempt has no retained outcome, preview reports the required typed abandonment disposition. Explicit abandonment records Failure with Unknown effect truth and never reruns validators; a later explicit proof starts a distinct attempt.",
          "Only the existing remote owner can permit its supported one-shot authenticated-absence retry. Merge and unsupported operation retries remain reconciliation-only. Multiple pending remote operations, or simultaneous local and remote uncertainty, are refused explicitly. With no pending work the result is a no-op.",
          "A completed semantic publication can retain an unsettled PR update transport. Recover authenticates its exact retained title, body, marker and head, then writes only the transport receipt. Completed semantic history is unchanged. Metadata mismatch, missing target or unavailable readback remains blocked: legacy update intents have no authenticated prior metadata, so these observations cannot safely authorize redispatch. The original retry budget and supported authenticated absence routes remain unchanged.",
          "Preserve native journals, intents and receipts. A changed token or request is not permission to bypass a guard. Source implementation requires focused installed proof and does not imply active binary replacement.",
          "The generated wrapper emitted by --emit-request can be passed directly to --intent-request; the inner request is also accepted."
        ],
        "OUTPUT": [
          "Machine JSON uses the common command-result envelope. Interpret status, authority, effects and findings independently. An emitted request is a snapshot, not execution. Foreign-platform snapshot rejection is not non-Unix execution proof."
        ],
        "SEE ALSO": [
          "csdlc(1), csdlc-requests(1), csdlc-workflow(1), docs/csdlc-v3/INTENT_COMMANDS.md."
        ],
        "OPTIONS": [
          "Supported candidate intent options: --disposition, --emit-request, --execute, --help, --intent-request, --json, --preview, --repo-root, -h. Positional ISSUE is positive; --help and -h are standalone discovery. Unsupported combinations fail closed."
        ],
        "EXAMPLES": [
          {
            "code": "csdlc recover ISSUE [--repo-root ROOT] [--json]\ncsdlc recover ISSUE --execute --preview DIGEST\ncsdlc recover ISSUE --disposition DISPOSITION.json --execute --preview DIGEST\ncsdlc recover --intent-request REQUEST.json"
          },
          "These forms require a supporting selected candidate; they do not upgrade the active binary or authorize activation."
        ]
      }
    }
  ]
}
