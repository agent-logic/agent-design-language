# One-time active-session notification

One-time operator-authorized ADL manual notice (#950, follow-up #861): PR #947 merged the reviewed 30-page manual at source head 4e816f87d61d11f48c950ededae75c6ca84f6258. That is source-review identity, not installed-version proof. Worker #9 observed no installed workflow page at the primary checkout's default .adl/bin/native-v3/share/man; other shells/custom destinations and binary provenance are unknown. From your selected repo root, the supported install is `bash adl/tools/install_owner_binaries.sh --bin csdlc`; default discovery is `export MANPATH="$PWD/.adl/bin/native-v3/share/man:${MANPATH:-}"`, then `man csdlc-workflow`. For custom destinations use the actual path printed by the installer. Check `man -w csdlc-workflow` and compare resolved pages with intended source before assuming version parity. Coordinate shared owner-binary replacement; this notice does not ask you to reinstall a live owner or interrupt your task. Worker #9 owns codex/950-manual-discovery in the matching FastWork issue worktree; root AGENTS pointer is being prepared for PR. Continue your existing work; no reply required.

Delivery: accepted by all five other discoverable active ADL Codex tasks. The sending Worker #9 has the same context. This is delivery-channel evidence, not acknowledgment or installation proof. Not-loaded tasks were excluded; external Claude/terminal sessions were not enumerable and are not claimed notified. Forward this text to those sessions as needed.
