# Gate 9 sample: small Rust lifecycle

## Boundary

Prove focused PVF selection and closeout for a small standalone Rust change.

## State-machine proof

The sample exercises `small-rust` through typed C-SDLC v2 operations. Cards are generated projections; Rust binaries alone own lifecycle mutation.

## Review questions

- Is the sample bounded and independently reproducible?
- Do failures remain fail-closed and resumable?
- Does evidence support only the observed outcome?
