# Gate 9 sample: hostile failures and recovery

## Boundary

Prove fail-closed retry, interruption, dirty-state, review, and remote recovery.

## State-machine proof

The sample exercises `hostile-recovery` through typed C-SDLC v2 operations. Cards are generated projections; Rust binaries alone own lifecycle mutation.

## Review questions

- Is the sample bounded and independently reproducible?
- Do failures remain fail-closed and resumable?
- Does evidence support only the observed outcome?
